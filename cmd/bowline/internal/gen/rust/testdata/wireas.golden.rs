// Code generated by bowline. DO NOT EDIT.

use bowline_client::{CallOptions, Empty, Error, Issue, Method, Transport, Validate, rules};
use serde::{Deserialize, Serialize};

#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct Envelope {
    pub kind: String,
}

impl Validate for Envelope {
    fn validate(&self, _path: &mut Vec<String>, _issues: &mut Vec<Issue>) {}
}

#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct Line {
    pub price: String,
    pub origin: [f64; 2],
    pub meta: Envelope,
    pub prices: Vec<String>,
}

impl Validate for Line {
    fn validate(&self, path: &mut Vec<String>, issues: &mut Vec<Issue>) {
        rules::nested(&self.meta, path, "meta", issues);
    }
}

/// Client calls the API described by the contract.
pub struct Client {
    transport: Transport,
}

impl Client {
    pub fn new(transport: Transport) -> Self {
        Self { transport }
    }

    pub fn transport(&self) -> &Transport {
        &self.transport
    }

    pub async fn get(&self, options: Option<&CallOptions>) -> Result<Line, Error> {
        self.transport
            .call("get", Method::Get, &Empty {}, options)
            .await
    }
}

