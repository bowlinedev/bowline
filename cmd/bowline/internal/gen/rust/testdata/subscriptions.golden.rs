// Code generated by bowline. DO NOT EDIT.

use bowline_client::{CallOptions, Error, Issue, Method, Stream, Transport, Validate, rules};
use serde::{Deserialize, Serialize};

/// Gone is the details shape of the Gone error variant (NOT_FOUND).
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct Gone {
    pub id: i64,
}

impl Validate for Gone {
    fn validate(&self, _path: &mut Vec<String>, _issues: &mut Vec<Issue>) {}
}

#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct Change {
    pub id: i64,
    pub at: chrono::DateTime<chrono::Utc>,
    pub tags: Vec<String>,
}

impl Validate for Change {
    fn validate(&self, _path: &mut Vec<String>, _issues: &mut Vec<Issue>) {}
}

#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct WatchInput {
    pub limit: i32,
}

impl Validate for WatchInput {
    fn validate(&self, path: &mut Vec<String>, issues: &mut Vec<Issue>) {
        rules::min_num(self.limit as f64, 1.0, "1", path, "limit", issues);
        rules::max_num(self.limit as f64, 100.0, "100", path, "limit", issues);
    }
}

/// Client calls the API described by the contract.
pub struct Client {
    transport: Transport,
}

impl Client {
    pub fn new(transport: Transport) -> Self {
        Self { transport }
    }

    pub fn transport(&self) -> &Transport {
        &self.transport
    }

    pub fn secret(&self, input: &WatchInput, options: Option<&CallOptions>) -> impl Stream<Item = Result<Change, Error>> + Send + 'static {
        self.transport
            .subscribe("secret", Method::Post, input, options)
    }

    /// Watch streams every change.
    pub fn watch(&self, input: &WatchInput, options: Option<&CallOptions>) -> impl Stream<Item = Result<Change, Error>> + Send + 'static {
        self.transport
            .subscribe("watch", Method::Get, input, options)
    }
}

