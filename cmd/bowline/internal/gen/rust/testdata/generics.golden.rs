// Code generated by bowline. DO NOT EDIT.

use bowline_client::{CallOptions, Error, Issue, Method, Transport, Validate, rules};
use serde::{Deserialize, Serialize};

/// Page is one page of results.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct Page<T> {
    pub items: Vec<T>,
    #[serde(default, skip_serializing_if = "Option::is_none")]
    pub next: Option<String>,
}

impl<T: Validate> Validate for Page<T> {
    fn validate(&self, path: &mut Vec<String>, issues: &mut Vec<Issue>) {
        rules::nested(&self.items, path, "items", issues);
    }
}

#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct Pair<K, V> {
    pub key: K,
    pub value: V,
}

impl<K: Validate, V: Validate> Validate for Pair<K, V> {
    fn validate(&self, path: &mut Vec<String>, issues: &mut Vec<Issue>) {
        rules::nested(&self.key, path, "key", issues);
        rules::nested(&self.value, path, "value", issues);
    }
}

/// RangeFloat64 is an instantiation of fidelity.test/rows/generics.Range.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct RangeFloat64 {
    pub low: f64,
    pub high: f64,
}

impl Validate for RangeFloat64 {
    fn validate(&self, _path: &mut Vec<String>, _issues: &mut Vec<Issue>) {}
}

/// RangeInt32 is an instantiation of fidelity.test/rows/generics.Range.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct RangeInt32 {
    pub low: i32,
    pub high: i32,
}

impl Validate for RangeInt32 {
    fn validate(&self, _path: &mut Vec<String>, _issues: &mut Vec<Issue>) {}
}

/// RangeInt64 is an instantiation of fidelity.test/rows/generics.Range.
#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct RangeInt64 {
    pub low: i64,
    pub high: i64,
}

impl Validate for RangeInt64 {
    fn validate(&self, _path: &mut Vec<String>, _issues: &mut Vec<Issue>) {}
}

#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct Report {
    pub users: Page<User>,
    pub nested: Page<Page<User>>,
    pub pairs: Vec<Pair<String, User>>,
    pub tree: Tree<User>,
    pub ints: RangeInt64,
    pub floats: RangeFloat64,
    pub scores: Page<RangeInt32>,
}

impl Validate for Report {
    fn validate(&self, path: &mut Vec<String>, issues: &mut Vec<Issue>) {
        rules::nested(&self.users, path, "users", issues);
        rules::nested(&self.nested, path, "nested", issues);
        rules::nested(&self.pairs, path, "pairs", issues);
        rules::nested(&self.tree, path, "tree", issues);
        rules::nested(&self.ints, path, "ints", issues);
        rules::nested(&self.floats, path, "floats", issues);
        rules::nested(&self.scores, path, "scores", issues);
    }
}

#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct Tree<T> {
    pub value: T,
    pub children: Vec<Tree<T>>,
}

impl<T: Validate> Validate for Tree<T> {
    fn validate(&self, path: &mut Vec<String>, issues: &mut Vec<Issue>) {
        rules::nested(&self.value, path, "value", issues);
        rules::nested(&self.children, path, "children", issues);
    }
}

#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct User {
    pub name: String,
}

impl Validate for User {
    fn validate(&self, _path: &mut Vec<String>, _issues: &mut Vec<Issue>) {}
}

/// Client calls the API described by the contract.
pub struct Client {
    transport: Transport,
}

impl Client {
    pub fn new(transport: Transport) -> Self {
        Self { transport }
    }

    pub fn transport(&self) -> &Transport {
        &self.transport
    }

    pub async fn get(&self, input: &Page<User>, options: Option<&CallOptions>) -> Result<Report, Error> {
        self.transport
            .call("get", Method::Get, input, options)
            .await
    }
}

