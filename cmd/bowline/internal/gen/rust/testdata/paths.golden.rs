// Code generated by bowline. DO NOT EDIT.

use bowline_client::{CallOptions, Error, Issue, Method, Transport, Validate, encode_segment, rules};
use serde::{Deserialize, Serialize};

#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct CreateInput {
    pub total: String,
}

impl Validate for CreateInput {
    fn validate(&self, path: &mut Vec<String>, issues: &mut Vec<Issue>) {
        rules::required_str(&self.total, path, "total", issues);
    }
}

#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct GetInput {
    pub id: i64,
}

impl Validate for GetInput {
    fn validate(&self, path: &mut Vec<String>, issues: &mut Vec<Issue>) {
        rules::required_int(self.id, path, "id", issues);
    }
}

#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct Invoice {
    pub id: i64,
    pub total: String,
}

impl Validate for Invoice {
    fn validate(&self, _path: &mut Vec<String>, _issues: &mut Vec<Issue>) {}
}

#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct LineInput {
    #[serde(rename = "invoiceId")]
    pub invoice_id: i64,
    #[serde(rename = "lineId")]
    pub line_id: String,
}

impl Validate for LineInput {
    fn validate(&self, path: &mut Vec<String>, issues: &mut Vec<Issue>) {
        rules::required_int(self.invoice_id, path, "invoiceId", issues);
        rules::required_str(&self.line_id, path, "lineId", issues);
    }
}

#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct ListInput {
    pub limit: i32,
}

impl Validate for ListInput {
    fn validate(&self, _path: &mut Vec<String>, _issues: &mut Vec<Issue>) {}
}

/// Client calls the API described by the contract.
pub struct Client {
    transport: Transport,
}

impl Client {
    pub fn new(transport: Transport) -> Self {
        Self { transport }
    }

    pub fn transport(&self) -> &Transport {
        &self.transport
    }

    pub fn invoices(&self) -> InvoicesClient<'_> {
        InvoicesClient {
            transport: &self.transport,
        }
    }
}

/// InvoicesClient groups the procedures under "invoices".
pub struct InvoicesClient<'a> {
    transport: &'a Transport,
}

impl<'a> InvoicesClient<'a> {
    pub async fn create(&self, input: &CreateInput, options: Option<&CallOptions>) -> Result<Invoice, Error> {
        self.transport
            .call_rest("invoices", Method::Post, input, &[], options)
            .await
    }

    pub async fn get(&self, input: &GetInput, options: Option<&CallOptions>) -> Result<Invoice, Error> {
        self.transport
            .call_rest(
                &format!("invoices/{}", encode_segment(&input.id)),
                Method::Get,
                input,
                &["id"],
                options,
            )
            .await
    }

    pub async fn line(&self, input: &LineInput, options: Option<&CallOptions>) -> Result<Invoice, Error> {
        self.transport
            .call_rest(
                &format!("invoices/{}/lines/{}", encode_segment(&input.invoice_id), encode_segment(&input.line_id)),
                Method::Get,
                input,
                &["invoiceId", "lineId"],
                options,
            )
            .await
    }

    pub async fn list(&self, input: &ListInput, options: Option<&CallOptions>) -> Result<Invoice, Error> {
        self.transport
            .call_rest("invoices", Method::Get, input, &[], options)
            .await
    }
}

