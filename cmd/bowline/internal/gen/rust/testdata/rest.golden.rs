// Code generated by bowline. DO NOT EDIT.

use bowline_client::{CallOptions, Error, Issue, Method, Transport, Validate, encode_segment, rules};
use serde::{Deserialize, Serialize};

#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct IdInput {
    pub id: i64,
}

impl Validate for IdInput {
    fn validate(&self, path: &mut Vec<String>, issues: &mut Vec<Issue>) {
        rules::required_int(self.id, path, "id", issues);
    }
}

#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct Invoice {
    pub id: i64,
    pub title: String,
}

impl Validate for Invoice {
    fn validate(&self, _path: &mut Vec<String>, _issues: &mut Vec<Issue>) {}
}

#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct ListInput {
    pub limit: i32,
    pub status: String,
    pub tags: Vec<String>,
    pub deep: bool,
}

impl Validate for ListInput {
    fn validate(&self, path: &mut Vec<String>, issues: &mut Vec<Issue>) {
        rules::min_num(self.limit as f64, 1.0, "1", path, "limit", issues);
        rules::max_num(self.limit as f64, 100.0, "100", path, "limit", issues);
    }
}

#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct RemoveInput {
    pub id: i64,
    pub force: bool,
}

impl Validate for RemoveInput {
    fn validate(&self, path: &mut Vec<String>, issues: &mut Vec<Issue>) {
        rules::required_int(self.id, path, "id", issues);
    }
}

#[derive(Clone, Debug, PartialEq, Serialize, Deserialize)]
pub struct ReplaceInput {
    pub id: i64,
    pub title: String,
}

impl Validate for ReplaceInput {
    fn validate(&self, path: &mut Vec<String>, issues: &mut Vec<Issue>) {
        rules::required_int(self.id, path, "id", issues);
        rules::required_str(&self.title, path, "title", issues);
    }
}

/// Client calls the API described by the contract.
pub struct Client {
    transport: Transport,
}

impl Client {
    pub fn new(transport: Transport) -> Self {
        Self { transport }
    }

    pub fn transport(&self) -> &Transport {
        &self.transport
    }

    pub fn invoices(&self) -> InvoicesClient<'_> {
        InvoicesClient {
            transport: &self.transport,
        }
    }
}

/// InvoicesClient groups the procedures under "invoices".
pub struct InvoicesClient<'a> {
    transport: &'a Transport,
}

impl<'a> InvoicesClient<'a> {
    pub async fn get(&self, input: &IdInput, options: Option<&CallOptions>) -> Result<Invoice, Error> {
        self.transport
            .call_rest(
                &format!("invoices/{}", encode_segment(&input.id)),
                Method::Get,
                input,
                &["id"],
                options,
            )
            .await
    }

    pub async fn list(&self, input: &ListInput, options: Option<&CallOptions>) -> Result<Invoice, Error> {
        self.transport
            .call_rest("invoices", Method::Get, input, &[], options)
            .await
    }

    pub async fn remove(&self, input: &RemoveInput, options: Option<&CallOptions>) -> Result<Invoice, Error> {
        self.transport
            .call_rest(
                &format!("invoices/{}", encode_segment(&input.id)),
                Method::Delete,
                input,
                &["id"],
                options,
            )
            .await
    }

    pub async fn replace(&self, input: &ReplaceInput, options: Option<&CallOptions>) -> Result<Invoice, Error> {
        self.transport
            .call_rest(
                &format!("invoices/{}", encode_segment(&input.id)),
                Method::Put,
                input,
                &["id"],
                options,
            )
            .await
    }
}

