# Code generated by bowline. DO NOT EDIT.

from __future__ import annotations

from typing import BinaryIO

from bowline_client import CallOptions, Method, SyncTransport, Transport
from pydantic import BaseModel, ConfigDict, Field


class AttachInput(BaseModel):
    model_config = ConfigDict(extra="ignore")

    invoiceId: int = Field(gt=0)


class Attachment(BaseModel):
    model_config = ConfigDict(extra="ignore")

    name: str
    size: int


class Client:
    """Transport client for every procedure in the contract."""

    def __init__(self, transport: Transport) -> None:
        self._transport = transport

    async def attach(
        self,
        input: AttachInput,
        file: BinaryIO,
        filename: str,
        options: CallOptions | None = None,
    ) -> Attachment:
        """Attach stores a file."""
        return await self._transport.upload("attach", input, file, filename, Attachment, options)


class SyncClient:
    """SyncTransport client for every procedure in the contract."""

    def __init__(self, transport: SyncTransport) -> None:
        self._transport = transport

    def attach(
        self,
        input: AttachInput,
        file: BinaryIO,
        filename: str,
        options: CallOptions | None = None,
    ) -> Attachment:
        """Attach stores a file."""
        return self._transport.upload("attach", input, file, filename, Attachment, options)
