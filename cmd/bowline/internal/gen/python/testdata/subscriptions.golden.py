# Code generated by bowline. DO NOT EDIT.

from __future__ import annotations

from collections.abc import AsyncIterator, Iterator
from datetime import datetime
from typing import Annotated

from bowline_client import CallOptions, Method, SyncTransport, Transport
from pydantic import BaseModel, ConfigDict, Field


class Change(BaseModel):
    model_config = ConfigDict(extra="ignore")

    id: int
    at: datetime
    tags: list[str]


class Gone(BaseModel):
    """Gone is the details shape of the Gone error variant (NOT_FOUND)."""

    model_config = ConfigDict(extra="ignore")

    id: int


class WatchInput(BaseModel):
    model_config = ConfigDict(extra="ignore")

    limit: int = Field(ge=1, le=100)


class Client:
    """Transport client for every procedure in the contract."""

    def __init__(self, transport: Transport) -> None:
        self._transport = transport

    def secret(
        self,
        input: WatchInput,
        options: CallOptions | None = None,
    ) -> AsyncIterator[Change]:
        return self._transport.subscribe("secret", input, Change, options, method=Method.POST)

    def watch(self, input: WatchInput, options: CallOptions | None = None) -> AsyncIterator[Change]:
        """Watch streams every change."""
        return self._transport.subscribe("watch", input, Change, options)


class SyncClient:
    """SyncTransport client for every procedure in the contract."""

    def __init__(self, transport: SyncTransport) -> None:
        self._transport = transport

    def secret(self, input: WatchInput, options: CallOptions | None = None) -> Iterator[Change]:
        return self._transport.subscribe("secret", input, Change, options, method=Method.POST)

    def watch(self, input: WatchInput, options: CallOptions | None = None) -> Iterator[Change]:
        """Watch streams every change."""
        return self._transport.subscribe("watch", input, Change, options)
