// Generated by bowline. DO NOT EDIT.

import 'package:bowline/bowline.dart';

class CreateInput {
  CreateInput({required this.name});

  factory CreateInput.fromJson(Map<String, Object?> json) => CreateInput(
        name: readString(json, 'name'),
      );

  final String name;

  Map<String, Object?> toJson() => {
        'name': name,
      };

  List<Issue> validate() => [
        if (name.isEmpty) Issue(['name'], 'required', 'is required'),
      ];
}

class Created {
  Created({required this.id});

  factory Created.fromJson(Map<String, Object?> json) => Created(
        id: readInt(json, 'id'),
      );

  final int id;

  Map<String, Object?> toJson() => {
        'id': id,
      };

  List<Issue> validate() => const [];
}

class Client {
  Client(this._transport);

  final Transport _transport;

  Future<Created> create(CreateInput input, {CallOptions? options}) {
    ensureValid(input.validate());
    return _transport.call('create', Method.post, input.toJson(), (json) => Created.fromJson(asObject(json)), options: options);
  }
}
