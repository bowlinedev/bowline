// Generated by bowline. DO NOT EDIT.

import 'package:bowline/bowline.dart';

class Node {
  Node({required this.name, required this.children, this.parent});

  factory Node.fromJson(Map<String, Object?> json) => Node(
        name: readString(json, 'name'),
        children: readList(json, 'children', (v0) => Node.fromJson(asObject(v0))),
        parent: asNullable(json['parent'], (v0) => Node.fromJson(asObject(v0))),
      );

  final String name;

  final List<Node> children;

  final Node? parent;

  Map<String, Object?> toJson() {
    final parent = this.parent;
    return {
        'name': name,
        'children': [for (final e0 in children) e0.toJson()],
        if (parent != null) 'parent': parent.toJson(),
    };
  }

  List<Issue> validate() {
    final parent = this.parent;
    return [
      for (final (i0, e0) in children.indexed) ...[...prefixed(['children', '$i0'], e0.validate())],
      if (parent != null) ...prefixed(['parent'], parent.validate()),
    ];
  }
}

class Client {
  Client(this._transport);

  final Transport _transport;

  Future<Node> get(Node input, {CallOptions? options}) {
    ensureValid(input.validate());
    return _transport.call('get', Method.get, input.toJson(), (json) => Node.fromJson(asObject(json)), options: options);
  }
}
