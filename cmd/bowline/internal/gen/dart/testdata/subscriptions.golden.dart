// Generated by bowline. DO NOT EDIT.

import 'package:bowline/bowline.dart';

class Change {
  Change({required this.id, required this.at, required this.tags});

  factory Change.fromJson(Map<String, Object?> json) => Change(
        id: readInt(json, 'id'),
        at: readTimestamp(json, 'at'),
        tags: readList(json, 'tags', asString),
      );

  final int id;

  final DateTime at;

  final List<String> tags;

  Map<String, Object?> toJson() => {
        'id': id,
        'at': encodeTimestamp(at),
        'tags': tags,
      };

  List<Issue> validate() => const [];
}

class WatchInput {
  WatchInput({required this.limit});

  factory WatchInput.fromJson(Map<String, Object?> json) => WatchInput(
        limit: readInt(json, 'limit'),
      );

  final int limit;

  Map<String, Object?> toJson() => {
        'limit': limit,
      };

  List<Issue> validate() => [
        if (limit < 1) Issue(['limit'], 'min', 'must be at least 1'),
        if (limit > 100) Issue(['limit'], 'max', 'must be at most 100'),
      ];
}

/// Gone is the details shape of the Gone error variant (NOT_FOUND).
class Gone {
  Gone({required this.id});

  factory Gone.fromJson(Map<String, Object?> json) => Gone(
        id: readInt(json, 'id'),
      );

  final int id;

  Map<String, Object?> toJson() => {
        'id': id,
      };

  List<Issue> validate() => const [];
}

class Client {
  Client(this._transport);

  final Transport _transport;

  Stream<Change> secret(WatchInput input, {CallOptions? options}) {
    ensureValid(input.validate());
    return _transport.subscribe('secret', input.toJson(), (json) => Change.fromJson(asObject(json)), options: options);
  }

  /// Watch streams every change.
  Stream<Change> watch(WatchInput input, {CallOptions? options}) {
    ensureValid(input.validate());
    return _transport.subscribe('watch', input.toJson(), (json) => Change.fromJson(asObject(json)), options: options);
  }
}
