// Generated by bowline. DO NOT EDIT.

import 'package:bowline/bowline.dart';

class Id {
  Id({required this.id});

  factory Id.fromJson(Map<String, Object?> json) => Id(
        id: readInt(json, 'id'),
      );

  final int id;

  Map<String, Object?> toJson() => {
        'id': id,
      };

  List<Issue> validate() => const [];
}

class Item {
  Item({required this.name});

  factory Item.fromJson(Map<String, Object?> json) => Item(
        name: readString(json, 'name'),
      );

  final String name;

  Map<String, Object?> toJson() => {
        'name': name,
      };

  List<Issue> validate() => const [];
}

class Client {
  Client(this._transport);

  final Transport _transport;

  /// Get returns one item.
  Future<Item> get(Id input, {CallOptions? options}) {
    ensureValid(input.validate());
    return _transport.call('get', Method.get, input.toJson(), (json) => Item.fromJson(asObject(json)), options: options);
  }

  Future<Item> hidden(Id input, {CallOptions? options}) {
    ensureValid(input.validate());
    return _transport.call('hidden', Method.get, input.toJson(), (json) => Item.fromJson(asObject(json)), options: options);
  }

  Future<void> remove(Id input, {CallOptions? options}) {
    ensureValid(input.validate());
    return _transport.call<void>('remove', Method.post, input.toJson(), (_) {}, options: options);
  }

  Future<Item> search(Item input, {CallOptions? options}) {
    ensureValid(input.validate());
    return _transport.call('search', Method.post, input.toJson(), (json) => Item.fromJson(asObject(json)), options: options);
  }
}
