// Generated by bowline. DO NOT EDIT.

import 'package:bowline/bowline.dart';

class Audit {
  Audit({required this.createdBy, required this.version});

  factory Audit.fromJson(Map<String, Object?> json) => Audit(
        createdBy: readString(json, 'createdBy'),
        version: readInt(json, 'version'),
      );

  final String createdBy;

  final int version;

  Map<String, Object?> toJson() => {
        'createdBy': createdBy,
        'version': version,
      };

  List<Issue> validate() => const [];
}

class Named {
  Named({required this.name});

  factory Named.fromJson(Map<String, Object?> json) => Named(
        name: readString(json, 'name'),
      );

  final String name;

  Map<String, Object?> toJson() => {
        'name': name,
      };

  List<Issue> validate() => const [];
}

class Record {
  Record({required this.id, required this.createdBy, required this.version, this.named, required this.extra});

  factory Record.fromJson(Map<String, Object?> json) => Record(
        id: readInt(json, 'id'),
        createdBy: readString(json, 'createdBy'),
        version: readInt(json, 'version'),
        named: asNullable(json['named'], (v0) => Named.fromJson(asObject(v0))),
        extra: readString(json, 'extra'),
      );

  final int id;

  final String createdBy;

  final int version;

  final Named? named;

  final String extra;

  Map<String, Object?> toJson() {
    final named = this.named;
    return {
        'id': id,
        'createdBy': createdBy,
        'version': version,
        'named': named == null ? null : named.toJson(),
        'extra': extra,
    };
  }

  List<Issue> validate() {
    final named = this.named;
    return [
      if (named != null) ...prefixed(['named'], named.validate()),
    ];
  }
}

class Client {
  Client(this._transport);

  final Transport _transport;

  Future<Record> get(Audit input, {CallOptions? options}) {
    ensureValid(input.validate());
    return _transport.call('get', Method.get, input.toJson(), (json) => Record.fromJson(asObject(json)), options: options);
  }
}
