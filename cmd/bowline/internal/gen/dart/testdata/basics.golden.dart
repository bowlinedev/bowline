// Generated by bowline. DO NOT EDIT.

import 'package:bowline/bowline.dart';

class Input {
  Input({required this.name, required this.count, required this.small, required this.big, required this.ratio, required this.on});

  factory Input.fromJson(Map<String, Object?> json) => Input(
        name: readString(json, 'name'),
        count: readInt(json, 'count'),
        small: readInt(json, 'small'),
        big: readInt(json, 'big'),
        ratio: readDouble(json, 'ratio'),
        on: readBool(json, 'on'),
      );

  final String name;

  final int count;

  final int small;

  final int big;

  final double ratio;

  final bool on;

  Map<String, Object?> toJson() => {
        'name': name,
        'count': count,
        'small': small,
        'big': big,
        'ratio': ratio,
        'on': on,
      };

  List<Issue> validate() => const [];
}

class Output {
  Output({required this.echo});

  factory Output.fromJson(Map<String, Object?> json) => Output(
        echo: readString(json, 'echo'),
      );

  final String echo;

  Map<String, Object?> toJson() => {
        'echo': echo,
      };

  List<Issue> validate() => const [];
}

class Client {
  Client(this._transport);

  final Transport _transport;

  Future<Output> echo(Input input, {CallOptions? options}) {
    ensureValid(input.validate());
    return _transport.call('echo', Method.get, input.toJson(), (json) => Output.fromJson(asObject(json)), options: options);
  }
}
