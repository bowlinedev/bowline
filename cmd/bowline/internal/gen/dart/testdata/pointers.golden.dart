// Generated by bowline. DO NOT EDIT.

import 'package:bowline/bowline.dart';

class Doc {
  Doc({this.title, this.subtitle, this.primary, required this.tags, required this.byName, this.count});

  factory Doc.fromJson(Map<String, Object?> json) => Doc(
        title: readOptionalString(json, 'title'),
        subtitle: readOptionalString(json, 'subtitle'),
        primary: asNullable(json['primary'], (v0) => Tag.fromJson(asObject(v0))),
        tags: readList(json, 'tags', (v0) => asNullable(v0, (v1) => Tag.fromJson(asObject(v1)))),
        byName: readMap(json, 'byName', (v0) => asNullable(v0, (v1) => Tag.fromJson(asObject(v1)))),
        count: readOptionalInt(json, 'count'),
      );

  final String? title;

  final String? subtitle;

  final Tag? primary;

  final List<Tag?> tags;

  final Map<String, Tag?> byName;

  final int? count;

  Map<String, Object?> toJson() {
    final primary = this.primary;
    return {
        'title': title,
        if (subtitle != null) 'subtitle': subtitle,
        'primary': primary == null ? null : primary.toJson(),
        'tags': [for (final e0 in tags) e0 == null ? null : e0.toJson()],
        'byName': {for (final MapEntry(key: k0, value: v0) in byName.entries) k0: v0 == null ? null : v0.toJson()},
        if (count != null) 'count': count,
    };
  }

  List<Issue> validate() {
    final primary = this.primary;
    return [
      if (primary != null) ...prefixed(['primary'], primary.validate()),
      for (final (i0, e0) in tags.indexed) ...[if (e0 != null) ...prefixed(['tags', '$i0'], e0.validate())],
      for (final MapEntry(key: k0, value: v0) in byName.entries) ...[if (v0 != null) ...prefixed(['byName', k0], v0.validate())],
    ];
  }
}

class Tag {
  Tag({required this.label});

  factory Tag.fromJson(Map<String, Object?> json) => Tag(
        label: readString(json, 'label'),
      );

  final String label;

  Map<String, Object?> toJson() => {
        'label': label,
      };

  List<Issue> validate() => const [];
}

class Client {
  Client(this._transport);

  final Transport _transport;

  Future<Doc> get(Tag input, {CallOptions? options}) {
    ensureValid(input.validate());
    return _transport.call('get', Method.get, input.toJson(), (json) => Doc.fromJson(asObject(json)), options: options);
  }
}
