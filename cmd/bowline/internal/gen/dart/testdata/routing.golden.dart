// Generated by bowline. DO NOT EDIT.

import 'package:bowline/bowline.dart';

class Item {
  Item({required this.name});

  factory Item.fromJson(Map<String, Object?> json) => Item(
        name: readString(json, 'name'),
      );

  final String name;

  Map<String, Object?> toJson() => {
        'name': name,
      };

  List<Issue> validate() => const [];
}

class RoutingId {
  RoutingId({required this.id});

  factory RoutingId.fromJson(Map<String, Object?> json) => RoutingId(
        id: readInt(json, 'id'),
      );

  final int id;

  Map<String, Object?> toJson() => {
        'id': id,
      };

  List<Issue> validate() => const [];
}

class SubId {
  SubId({required this.id});

  factory SubId.fromJson(Map<String, Object?> json) => SubId(
        id: readInt(json, 'id'),
      );

  final int id;

  Map<String, Object?> toJson() => {
        'id': id,
      };

  List<Issue> validate() => const [];
}

class AdminClient {
  AdminClient(this._transport);

  final Transport _transport;

  @Deprecated('use sub.remove')
  Future<void> purge(RoutingId input, {CallOptions? options}) {
    ensureValid(input.validate());
    return _transport.call<void>('admin.purge', Method.post, input.toJson(), (_) {}, options: options);
  }
}

class SubClient {
  SubClient(this._transport);

  final Transport _transport;

  /// Remove deletes an item by ID.
  Future<void> remove(SubId input, {CallOptions? options}) {
    ensureValid(input.validate());
    return _transport.call<void>('sub.remove', Method.post, input.toJson(), (_) {}, options: options);
  }
}

class Client {
  Client(Transport transport)
      : _transport = transport,
        admin = AdminClient(transport),
        sub = SubClient(transport);

  final Transport _transport;

  final AdminClient admin;

  final SubClient sub;

  /// Get fetches an item.
  Future<Item> get(RoutingId input, {CallOptions? options}) {
    ensureValid(input.validate());
    return _transport.call('get', Method.get, input.toJson(), (json) => Item.fromJson(asObject(json)), options: options);
  }

  Future<Item> search(Item input, {CallOptions? options}) {
    ensureValid(input.validate());
    return _transport.call('search', Method.post, input.toJson(), (json) => Item.fromJson(asObject(json)), options: options);
  }
}
