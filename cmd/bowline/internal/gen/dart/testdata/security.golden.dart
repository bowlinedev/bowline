// Generated by bowline. DO NOT EDIT.

import 'package:bowline/bowline.dart';

class In {
  In({required this.id});

  factory In.fromJson(Map<String, Object?> json) => In(
        id: readInt(json, 'id'),
      );

  final int id;

  Map<String, Object?> toJson() => {
        'id': id,
      };

  List<Issue> validate() => const [];
}

class Client {
  Client(this._transport);

  final Transport _transport;

  Future<In> admin(In input, {CallOptions? options}) {
    ensureValid(input.validate());
    return _transport.call('admin', Method.post, input.toJson(), (json) => In.fromJson(asObject(json)), options: options);
  }

  Future<In> get(In input, {CallOptions? options}) {
    ensureValid(input.validate());
    return _transport.call('get', Method.get, input.toJson(), (json) => In.fromJson(asObject(json)), options: options);
  }

  Future<In> ping(In input, {CallOptions? options}) {
    ensureValid(input.validate());
    return _transport.call('ping', Method.get, input.toJson(), (json) => In.fromJson(asObject(json)), options: options);
  }
}
