// Generated by bowline. DO NOT EDIT.

import 'package:bowline/bowline.dart';

class Id {
  Id({required this.id});

  factory Id.fromJson(Map<String, Object?> json) => Id(
        id: readInt(json, 'id'),
      );

  final int id;

  Map<String, Object?> toJson() => {
        'id': id,
      };

  List<Issue> validate() => const [];
}

class Invoice {
  Invoice({required this.id});

  factory Invoice.fromJson(Map<String, Object?> json) => Invoice(
        id: readInt(json, 'id'),
      );

  final int id;

  Map<String, Object?> toJson() => {
        'id': id,
      };

  List<Issue> validate() => const [];
}

enum Status {
  draft('draft'),
  paid('paid');

  const Status(this.value);

  final String value;

  static Status fromValue(Object? value) => values.firstWhere(
        (v) => v.value == value,
        orElse: () => throw FormatException('Status: unknown value $value'),
      );
}

/// InvoiceLocked is returned when an invoice cannot change.
class InvoiceLocked {
  InvoiceLocked({required this.id, required this.status, required this.since});

  factory InvoiceLocked.fromJson(Map<String, Object?> json) => InvoiceLocked(
        id: readInt(json, 'id'),
        status: Status.fromValue(json['status']),
        since: readTimestamp(json, 'since'),
      );

  final int id;

  final Status status;

  final DateTime since;

  Map<String, Object?> toJson() => {
        'id': id,
        'status': status.value,
        'since': encodeTimestamp(since),
      };

  List<Issue> validate() => const [];
}

/// QuotaExceeded is the details shape of the QuotaExceeded error variant (RESOURCE_EXHAUSTED).
class QuotaExceeded {
  QuotaExceeded({required this.limit});

  factory QuotaExceeded.fromJson(Map<String, Object?> json) => QuotaExceeded(
        limit: readInt(json, 'limit'),
      );

  final int limit;

  Map<String, Object?> toJson() => {
        'limit': limit,
      };

  List<Issue> validate() => const [];
}

class Client {
  Client(this._transport);

  final Transport _transport;

  Future<Invoice> send(Id input, {CallOptions? options}) {
    ensureValid(input.validate());
    return _transport.call('send', Method.post, input.toJson(), (json) => Invoice.fromJson(asObject(json)), options: options);
  }

  Future<Invoice> void_(Id input, {CallOptions? options}) {
    ensureValid(input.validate());
    return _transport.call('void', Method.post, input.toJson(), (json) => Invoice.fromJson(asObject(json)), options: options);
  }
}
