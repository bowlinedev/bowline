// Generated by bowline. DO NOT EDIT.

import 'package:bowline/bowline.dart';

class Envelope {
  Envelope({required this.kind});

  factory Envelope.fromJson(Map<String, Object?> json) => Envelope(
        kind: readString(json, 'kind'),
      );

  final String kind;

  Map<String, Object?> toJson() => {
        'kind': kind,
      };

  List<Issue> validate() => const [];
}

class Line {
  Line({required this.price, required this.origin, required this.meta, required this.prices});

  factory Line.fromJson(Map<String, Object?> json) => Line(
        price: readString(json, 'price'),
        origin: readList(json, 'origin', asDouble),
        meta: Envelope.fromJson(asObject(json['meta'])),
        prices: readList(json, 'prices', asString),
      );

  final String price;

  final List<double> origin;

  final Envelope meta;

  final List<String> prices;

  Map<String, Object?> toJson() => {
        'price': price,
        'origin': origin,
        'meta': meta.toJson(),
        'prices': prices,
      };

  List<Issue> validate() => [
        if (origin.length != 2) Issue(['origin'], 'len', 'must be exactly 2 items'),
        ...prefixed(['meta'], meta.validate()),
      ];
}

class Client {
  Client(this._transport);

  final Transport _transport;

  Future<Line> get({CallOptions? options}) {
    return _transport.call('get', Method.get, const {}, (json) => Line.fromJson(asObject(json)), options: options);
  }
}
