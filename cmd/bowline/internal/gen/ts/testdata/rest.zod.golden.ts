// Code generated by bowline. DO NOT EDIT.

import { z } from "zod";

const IDInput = z.object({
  id: z.number().int(),
});

const Invoice = z.object({
  id: z.number().int(),
  title: z.string(),
});

const ListInput = z.object({
  limit: z.number().int().min(1).max(100),
  status: z.string(),
  tags: z.array(z.string()),
  deep: z.boolean(),
});

const RemoveInput = z.object({
  id: z.number().int(),
  force: z.boolean(),
});

const ReplaceInput = z.object({
  id: z.number().int(),
  title: z.string().min(1),
});

export const schemas = {
  IDInput,
  Invoice,
  ListInput,
  RemoveInput,
  ReplaceInput,
} as const;

export const inputs = {
  "invoices.get": IDInput,
  "invoices.list": ListInput,
  "invoices.remove": RemoveInput,
  "invoices.replace": ReplaceInput,
} as const;

export const errors = {
} as const;
