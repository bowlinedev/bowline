// Code generated by bowline. DO NOT EDIT.

import { createClient as create, type ClientOptions, type ContractRuntime, type BowlineError, type Mutation, type Query } from "@bowlinedev/client";

export interface IDInput {
  id: number;
}

export interface Invoice {
  id: number;
  title: string;
}

export interface ListInput {
  limit: number;
  status: string;
  tags: string[];
  deep: boolean;
}

export interface RemoveInput {
  id: number;
  force: boolean;
}

export interface ReplaceInput {
  id: number;
  title: string;
}

export interface Errors {
  "invoices.get": BowlineError;
  "invoices.list": BowlineError;
  "invoices.remove": BowlineError;
  "invoices.replace": BowlineError;
}

export type ProcedureError<P extends keyof Errors> = Errors[P];

export interface Client {
  invoices: {
    get: Query<IDInput, Invoice>;
    list: Query<ListInput, Invoice>;
    remove: Mutation<RemoveInput, Invoice>;
    replace: Mutation<ReplaceInput, Invoice>;
  };
}

export const contract = {
  version: "1.4",
  hydrators: {},
  procedures: {
    "invoices.get": { kind: "query", method: "GET", path: "invoices/{id}" },
    "invoices.list": { kind: "query", method: "GET", path: "invoices" },
    "invoices.remove": { kind: "mutation", method: "DELETE", path: "invoices/{id}" },
    "invoices.replace": { kind: "mutation", method: "PUT", path: "invoices/{id}" },
  },
} satisfies ContractRuntime;

export function createClient(options: ClientOptions): Client {
  return create(contract, options) as Client;
}
