// Code generated by bowline. DO NOT EDIT.

import { z } from "zod";

const CreateInput = z.object({
  total: z.string().min(1),
});

const GetInput = z.object({
  id: z.number().int(),
});

const Invoice = z.object({
  id: z.number().int(),
  total: z.string(),
});

const LineInput = z.object({
  invoiceId: z.number().int(),
  lineId: z.string().min(1),
});

const ListInput = z.object({
  limit: z.number().int(),
});

export const schemas = {
  CreateInput,
  GetInput,
  Invoice,
  LineInput,
  ListInput,
} as const;

export const inputs = {
  "invoices.create": CreateInput,
  "invoices.get": GetInput,
  "invoices.line": LineInput,
  "invoices.list": ListInput,
} as const;

export const errors = {
} as const;
