# Code generated by bowline. DO NOT EDIT.

defmodule Golden.Examples.Types do
  @moduledoc "Named primitive types declared by the contract."

  @type slug :: String.t()
end

defmodule Golden.Examples.Types.Address do
  @moduledoc false

  alias BowlineClient.Read

  @type t :: %__MODULE__{
          city: String.t()
        }

  defstruct [
    :city
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Address")

    %__MODULE__{
      city: Read.string_value(Map.get(map, "city"), "city")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "city" => v.city
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Examples.Types.Level do
  @moduledoc false

  alias BowlineClient.Read

  @type t :: 1 | 10

  @spec from_value(term()) :: t()
  def from_value(1), do: 1
  def from_value(10), do: 10

  def from_value(other) do
    Read.mismatch("Level", "one of 1, 10", other)
  end

  @spec to_value(t()) :: integer()
  def to_value(1), do: 1
  def to_value(10), do: 10
end

defmodule Golden.Examples.Types.Sample do
  @moduledoc false

  alias BowlineClient.Encode
  alias BowlineClient.Read
  alias BowlineClient.Rules
  alias Golden.Examples.Types

  @type t :: %__MODULE__{
          name: String.t(),
          email: String.t(),
          age: integer(),
          big: integer(),
          ratio: float(),
          active: boolean(),
          status: Types.Status.t(),
          level: Types.Level.t(),
          slug: Types.slug(),
          since: DateTime.t(),
          timeout: integer(),
          blob: binary(),
          raw: term(),
          tags: [String.t()],
          counts: %{optional(String.t()) => integer()},
          home: Types.Address.t(),
          nick: String.t() | nil,
          ratings: [Types.Level.t()]
        }

  defstruct [
    :name,
    :email,
    :age,
    :big,
    :ratio,
    :active,
    :status,
    :level,
    :slug,
    :since,
    :timeout,
    :blob,
    :raw,
    :tags,
    :counts,
    :home,
    :nick,
    :ratings
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Sample")

    %__MODULE__{
      name: Read.string_value(Map.get(map, "name"), "name"),
      email: Read.string_value(Map.get(map, "email"), "email"),
      age: Read.int_value(Map.get(map, "age"), "age"),
      big: Read.big_int_value(Map.get(map, "big"), "big"),
      ratio: Read.float_value(Map.get(map, "ratio"), "ratio"),
      active: Read.bool_value(Map.get(map, "active"), "active"),
      status: Types.Status.from_value(Map.get(map, "status")),
      level: Types.Level.from_value(Map.get(map, "level")),
      slug: Read.string_value(Map.get(map, "slug"), "slug"),
      since: Read.timestamp_value(Map.get(map, "since"), "since"),
      timeout: Read.int_value(Map.get(map, "timeout"), "timeout"),
      blob: Read.bytes_value(Map.get(map, "blob"), "blob"),
      raw: Map.get(map, "raw"),
      tags: Read.list_value(Map.get(map, "tags"), "tags", &Read.string_value(&1, "tags")),
      counts: Read.map_value(Map.get(map, "counts"), "counts", &Read.int_value(&1, "counts")),
      home: Types.Address.from_map(Map.get(map, "home")),
      nick: Read.optional_value(Map.get(map, "nick"), &Read.string_value(&1, "nick")),
      ratings: Read.list_value(Map.get(map, "ratings"), "ratings", &Types.Level.from_value/1)
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "name" => v.name,
      "email" => v.email,
      "age" => v.age,
      "big" => Encode.big_int(v.big),
      "ratio" => v.ratio,
      "active" => v.active,
      "status" => Types.Status.to_value(v.status),
      "level" => Types.Level.to_value(v.level),
      "slug" => v.slug,
      "since" => Encode.timestamp(v.since),
      "timeout" => v.timeout,
      "blob" => Encode.bytes(v.blob),
      "raw" => v.raw,
      "tags" => Encode.list(v.tags, &Function.identity/1),
      "counts" => Encode.map(v.counts, &Function.identity/1),
      "home" => Types.Address.to_map(v.home),
      "ratings" => Encode.list(v.ratings, &Types.Level.to_value/1)
    }
    |> Encode.optional("nick", v.nick, &Function.identity/1)
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{} = v) do
    List.flatten([
      Rules.required(["name"], v.name),
      Rules.email(["email"], v.email),
      Rules.min(["age"], v.age, 0),
      Read.prefixed(["home"], Types.Address.validate(v.home))
    ])
  end
end

defmodule Golden.Examples.Types.Status do
  @moduledoc false

  alias BowlineClient.Read

  @type t :: :draft | :sent

  @spec from_value(term()) :: t()
  def from_value("draft"), do: :draft
  def from_value("sent"), do: :sent

  def from_value(other) do
    Read.mismatch("Status", "one of draft, sent", other)
  end

  @spec to_value(t()) :: String.t()
  def to_value(:draft), do: "draft"
  def to_value(:sent), do: "sent"
end

defmodule Golden.Examples.Client do
  @moduledoc """
  Entry point for the API: `new/2` builds the transport every module takes first.
  """

  alias BowlineClient.Transport
  alias Golden.Examples.Types

  @spec new(String.t(), keyword()) :: Transport.t()
  def new(base_url, opts \\ []) do
    Transport.new(Keyword.put(opts, :base_url, base_url))
  end

  @doc """
  get calls get.
  """
  @spec get(Transport.t(), Transport.call_opts()) ::
          {:ok, Types.Sample.t()} | {:error, BowlineClient.Error.t()}
  def get(transport, opts \\ []) do
    Transport.call(transport, "get", :get, %{}, &Types.Sample.from_map/1, opts)
  end

  @spec get!(Transport.t(), Transport.call_opts()) :: Types.Sample.t()
  def get!(transport, opts \\ []) do
    case get(transport, opts) do
      {:ok, value} -> value
      {:error, error} -> raise error
    end
  end
end
