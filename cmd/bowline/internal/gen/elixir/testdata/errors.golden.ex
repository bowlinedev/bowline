# Code generated by bowline. DO NOT EDIT.

defmodule Golden.Errors.Types.Id do
  @moduledoc false

  alias BowlineClient.Read

  @type t :: %__MODULE__{
          id: integer()
        }

  defstruct [
    :id
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Id")

    %__MODULE__{
      id: Read.int_value(Map.get(map, "id"), "id")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "id" => v.id
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Errors.Types.Invoice do
  @moduledoc false

  alias BowlineClient.Read

  @type t :: %__MODULE__{
          id: integer()
        }

  defstruct [
    :id
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Invoice")

    %__MODULE__{
      id: Read.int_value(Map.get(map, "id"), "id")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "id" => v.id
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Errors.Types.Status do
  @moduledoc false

  alias BowlineClient.Read

  @type t :: :draft | :paid

  @spec from_value(term()) :: t()
  def from_value("draft"), do: :draft
  def from_value("paid"), do: :paid

  def from_value(other) do
    Read.mismatch("Status", "one of draft, paid", other)
  end

  @spec to_value(t()) :: String.t()
  def to_value(:draft), do: "draft"
  def to_value(:paid), do: "paid"
end

defmodule Golden.Errors.Types.InvoiceLocked do
  @moduledoc """
  InvoiceLocked is returned when an invoice cannot change.
  """

  alias BowlineClient.Encode
  alias BowlineClient.Read
  alias Golden.Errors.Types

  @type t :: %__MODULE__{
          id: integer(),
          status: Types.Status.t(),
          since: DateTime.t()
        }

  defstruct [
    :id,
    :status,
    :since
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "InvoiceLocked")

    %__MODULE__{
      id: Read.int_value(Map.get(map, "id"), "id"),
      status: Types.Status.from_value(Map.get(map, "status")),
      since: Read.timestamp_value(Map.get(map, "since"), "since")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "id" => v.id,
      "status" => Types.Status.to_value(v.status),
      "since" => Encode.timestamp(v.since)
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Errors.Types.QuotaExceeded do
  @moduledoc """
  QuotaExceeded is the details shape of the QuotaExceeded error variant (RESOURCE_EXHAUSTED).
  """

  alias BowlineClient.Read

  @type t :: %__MODULE__{
          limit: integer()
        }

  defstruct [
    :limit
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "QuotaExceeded")

    %__MODULE__{
      limit: Read.int_value(Map.get(map, "limit"), "limit")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "limit" => v.limit
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Errors.Client do
  @moduledoc """
  Entry point for the API: `new/2` builds the transport every module takes first.
  """

  alias BowlineClient.Transport
  alias Golden.Errors.Types

  @spec new(String.t(), keyword()) :: Transport.t()
  def new(base_url, opts \\ []) do
    Transport.new(Keyword.put(opts, :base_url, base_url))
  end

  @doc """
  send calls send.
  """
  @spec send(Transport.t(), Types.Id.t(), Transport.call_opts()) ::
          {:ok, Types.Invoice.t()} | {:error, BowlineClient.Error.t()}
  def send(transport, %Types.Id{} = input, opts \\ []) do
    Transport.call(
      transport,
      "send",
      :post,
      Types.Id.to_map(input),
      &Types.Invoice.from_map/1,
      opts
    )
  end

  @spec send!(Transport.t(), Types.Id.t(), Transport.call_opts()) :: Types.Invoice.t()
  def send!(transport, input, opts \\ []) do
    case send(transport, input, opts) do
      {:ok, value} -> value
      {:error, error} -> raise error
    end
  end

  @doc """
  void calls void.
  """
  @spec void(Transport.t(), Types.Id.t(), Transport.call_opts()) ::
          {:ok, Types.Invoice.t()} | {:error, BowlineClient.Error.t()}
  def void(transport, %Types.Id{} = input, opts \\ []) do
    Transport.call(
      transport,
      "void",
      :post,
      Types.Id.to_map(input),
      &Types.Invoice.from_map/1,
      opts
    )
  end

  @spec void!(Transport.t(), Types.Id.t(), Transport.call_opts()) :: Types.Invoice.t()
  def void!(transport, input, opts \\ []) do
    case void(transport, input, opts) do
      {:ok, value} -> value
      {:error, error} -> raise error
    end
  end

  @doc "Decodes the details of a declared error variant into its struct; nil for other errors."
  @spec error_details(BowlineClient.Error.t()) :: struct() | nil
  def error_details(%BowlineClient.Error{type: "InvoiceLocked", details: details})
      when is_map(details) do
    Types.InvoiceLocked.from_map(details)
  end

  def error_details(%BowlineClient.Error{type: "QuotaExceeded", details: details})
      when is_map(details) do
    Types.QuotaExceeded.from_map(details)
  end

  def error_details(%BowlineClient.Error{}), do: nil
end
