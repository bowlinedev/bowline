# Code generated by bowline. DO NOT EDIT.

defmodule Golden.Aliases.Types.Holder do
  @moduledoc false

  alias BowlineClient.Encode
  alias BowlineClient.Read
  alias Golden.Aliases.Types

  @type t :: %__MODULE__{
          a: Types.Real.t(),
          b: [Types.Real.t()]
        }

  defstruct [
    :a,
    :b
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Holder")

    %__MODULE__{
      a: Types.Real.from_map(Map.get(map, "a")),
      b: Read.list_value(Map.get(map, "b"), "b", &Types.Real.from_map/1)
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "a" => Types.Real.to_map(v.a),
      "b" => Encode.list(v.b, &Types.Real.to_map/1)
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{} = v) do
    List.flatten([
      Read.prefixed(["a"], Types.Real.validate(v.a)),
      Read.validate_list(["b"], v.b, &Types.Real.validate/1)
    ])
  end
end

defmodule Golden.Aliases.Types.Real do
  @moduledoc false

  alias BowlineClient.Read

  @type t :: %__MODULE__{
          n: integer()
        }

  defstruct [
    :n
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Real")

    %__MODULE__{
      n: Read.int_value(Map.get(map, "n"), "n")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "n" => v.n
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Aliases.Client do
  @moduledoc """
  Entry point for the API: `new/2` builds the transport every module takes first.
  """

  alias BowlineClient.Transport
  alias Golden.Aliases.Types

  @spec new(String.t(), keyword()) :: Transport.t()
  def new(base_url, opts \\ []) do
    Transport.new(Keyword.put(opts, :base_url, base_url))
  end

  @doc """
  get calls get.
  """
  @spec get(Transport.t(), Types.Real.t(), Transport.call_opts()) ::
          {:ok, Types.Holder.t()} | {:error, BowlineClient.Error.t()}
  def get(transport, %Types.Real{} = input, opts \\ []) do
    Transport.call(
      transport,
      "get",
      :get,
      Types.Real.to_map(input),
      &Types.Holder.from_map/1,
      opts
    )
  end

  @spec get!(Transport.t(), Types.Real.t(), Transport.call_opts()) :: Types.Holder.t()
  def get!(transport, input, opts \\ []) do
    case get(transport, input, opts) do
      {:ok, value} -> value
      {:error, error} -> raise error
    end
  end
end
