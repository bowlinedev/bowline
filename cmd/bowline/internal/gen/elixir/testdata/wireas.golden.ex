# Code generated by bowline. DO NOT EDIT.

defmodule Golden.Wireas.Types.Envelope do
  @moduledoc false

  alias BowlineClient.Read

  @type t :: %__MODULE__{
          kind: String.t()
        }

  defstruct [
    :kind
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Envelope")

    %__MODULE__{
      kind: Read.string_value(Map.get(map, "kind"), "kind")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "kind" => v.kind
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Wireas.Types.Line do
  @moduledoc false

  alias BowlineClient.Encode
  alias BowlineClient.Read
  alias Golden.Wireas.Types

  @type t :: %__MODULE__{
          price: String.t(),
          origin: [float()],
          meta: Types.Envelope.t(),
          prices: [String.t()]
        }

  defstruct [
    :price,
    :origin,
    :meta,
    :prices
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Line")

    %__MODULE__{
      price: Read.string_value(Map.get(map, "price"), "price"),
      origin: Read.list_value(Map.get(map, "origin"), "origin", &Read.float_value(&1, "origin")),
      meta: Types.Envelope.from_map(Map.get(map, "meta")),
      prices: Read.list_value(Map.get(map, "prices"), "prices", &Read.string_value(&1, "prices"))
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "price" => v.price,
      "origin" => Encode.list(v.origin, &Function.identity/1),
      "meta" => Types.Envelope.to_map(v.meta),
      "prices" => Encode.list(v.prices, &Function.identity/1)
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{} = v) do
    List.flatten([
      Read.prefixed(["meta"], Types.Envelope.validate(v.meta))
    ])
  end
end

defmodule Golden.Wireas.Client do
  @moduledoc """
  Entry point for the API: `new/2` builds the transport every module takes first.
  """

  alias BowlineClient.Transport
  alias Golden.Wireas.Types

  @spec new(String.t(), keyword()) :: Transport.t()
  def new(base_url, opts \\ []) do
    Transport.new(Keyword.put(opts, :base_url, base_url))
  end

  @doc """
  get calls get.
  """
  @spec get(Transport.t(), Transport.call_opts()) ::
          {:ok, Types.Line.t()} | {:error, BowlineClient.Error.t()}
  def get(transport, opts \\ []) do
    Transport.call(transport, "get", :get, %{}, &Types.Line.from_map/1, opts)
  end

  @spec get!(Transport.t(), Transport.call_opts()) :: Types.Line.t()
  def get!(transport, opts \\ []) do
    case get(transport, opts) do
      {:ok, value} -> value
      {:error, error} -> raise error
    end
  end
end
