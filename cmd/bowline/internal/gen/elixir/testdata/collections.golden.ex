# Code generated by bowline. DO NOT EDIT.

defmodule Golden.Collections.Types.Cell do
  @moduledoc false

  alias BowlineClient.Read

  @type t :: %__MODULE__{
          v: integer()
        }

  defstruct [
    :v
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Cell")

    %__MODULE__{
      v: Read.int_value(Map.get(map, "v"), "v")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "v" => v.v
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Collections.Types.Grid do
  @moduledoc false

  alias BowlineClient.Encode
  alias BowlineClient.Read
  alias BowlineClient.Rules
  alias Golden.Collections.Types

  @type t :: %__MODULE__{
          rows: [[Types.Cell.t()]],
          fixed: [integer()],
          by_key: %{optional(String.t()) => Types.Cell.t()},
          by_int: %{optional(String.t()) => String.t()},
          names: [String.t()] | nil,
          blob: binary(),
          ids: [integer()]
        }

  defstruct [
    :rows,
    :fixed,
    :by_key,
    :by_int,
    :names,
    :blob,
    :ids
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Grid")

    %__MODULE__{
      rows: Read.list_value(Map.get(map, "rows"), "rows", &decode_rows/1),
      fixed: Read.list_value(Map.get(map, "fixed"), "fixed", &Read.int_value(&1, "fixed")),
      by_key: Read.map_value(Map.get(map, "byKey"), "byKey", &Types.Cell.from_map/1),
      by_int: Read.map_value(Map.get(map, "byInt"), "byInt", &Read.string_value(&1, "byInt")),
      names: Read.optional_value(Map.get(map, "names"), &decode_names/1),
      blob: Read.bytes_value(Map.get(map, "blob"), "blob"),
      ids: Read.list_value(Map.get(map, "ids"), "ids", &Read.int_value(&1, "ids"))
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "rows" => Encode.list(v.rows, &encode_rows/1),
      "fixed" => Encode.list(v.fixed, &Function.identity/1),
      "byKey" => Encode.map(v.by_key, &Types.Cell.to_map/1),
      "byInt" => Encode.map(v.by_int, &Function.identity/1),
      "blob" => Encode.bytes(v.blob),
      "ids" => Encode.list(v.ids, &Function.identity/1)
    }
    |> Encode.optional("names", v.names, &encode_names/1)
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{} = v) do
    List.flatten([
      Read.validate_list(["rows"], v.rows, &validate_rows/1),
      Read.validate_map(["byKey"], v.by_key, &Types.Cell.validate/1),
      Rules.max(["names"], v.names, 10)
    ])
  end

  defp decode_rows(value) do
    Read.list_value(value, "rows", &Types.Cell.from_map/1)
  end

  defp decode_names(value) do
    Read.list_value(value, "names", &Read.string_value(&1, "names"))
  end

  defp encode_rows(value) do
    Encode.list(value, &Types.Cell.to_map/1)
  end

  defp encode_names(value) do
    Encode.list(value, &Function.identity/1)
  end

  defp validate_rows(value) do
    Read.validate_list([], value, &Types.Cell.validate/1)
  end
end

defmodule Golden.Collections.Client do
  @moduledoc """
  Entry point for the API: `new/2` builds the transport every module takes first.
  """

  alias BowlineClient.Transport
  alias Golden.Collections.Types

  @spec new(String.t(), keyword()) :: Transport.t()
  def new(base_url, opts \\ []) do
    Transport.new(Keyword.put(opts, :base_url, base_url))
  end

  @doc """
  get calls get.
  """
  @spec get(Transport.t(), Types.Cell.t(), Transport.call_opts()) ::
          {:ok, Types.Grid.t()} | {:error, BowlineClient.Error.t()}
  def get(transport, %Types.Cell{} = input, opts \\ []) do
    Transport.call(transport, "get", :get, Types.Cell.to_map(input), &Types.Grid.from_map/1, opts)
  end

  @spec get!(Transport.t(), Types.Cell.t(), Transport.call_opts()) :: Types.Grid.t()
  def get!(transport, input, opts \\ []) do
    case get(transport, input, opts) do
      {:ok, value} -> value
      {:error, error} -> raise error
    end
  end
end
