# Code generated by bowline. DO NOT EDIT.

defmodule Golden.Stdlib.Types.Event do
  @moduledoc false

  alias BowlineClient.Encode
  alias BowlineClient.Read

  @type t :: %__MODULE__{
          at: DateTime.t(),
          took: integer(),
          payload: term(),
          addr: String.t(),
          big: integer(),
          unsigned: integer()
        }

  defstruct [
    :at,
    :took,
    :payload,
    :addr,
    :big,
    :unsigned
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Event")

    %__MODULE__{
      at: Read.timestamp_value(Map.get(map, "at"), "at"),
      took: Read.int_value(Map.get(map, "took"), "took"),
      payload: Map.get(map, "payload"),
      addr: Read.string_value(Map.get(map, "addr"), "addr"),
      big: Read.big_int_value(Map.get(map, "big"), "big"),
      unsigned: Read.big_int_value(Map.get(map, "unsigned"), "unsigned")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "at" => Encode.timestamp(v.at),
      "took" => v.took,
      "payload" => v.payload,
      "addr" => v.addr,
      "big" => Encode.big_int(v.big),
      "unsigned" => Encode.big_int(v.unsigned)
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Stdlib.Client do
  @moduledoc """
  Entry point for the API: `new/2` builds the transport every module takes first.
  """

  alias BowlineClient.Transport
  alias Golden.Stdlib.Types

  @spec new(String.t(), keyword()) :: Transport.t()
  def new(base_url, opts \\ []) do
    Transport.new(Keyword.put(opts, :base_url, base_url))
  end

  @doc """
  get calls get.
  """
  @spec get(Transport.t(), Transport.call_opts()) ::
          {:ok, Types.Event.t()} | {:error, BowlineClient.Error.t()}
  def get(transport, opts \\ []) do
    Transport.call(transport, "get", :get, %{}, &Types.Event.from_map/1, opts)
  end

  @spec get!(Transport.t(), Transport.call_opts()) :: Types.Event.t()
  def get!(transport, opts \\ []) do
    case get(transport, opts) do
      {:ok, value} -> value
      {:error, error} -> raise error
    end
  end
end
