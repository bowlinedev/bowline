# Code generated by bowline. DO NOT EDIT.

defmodule Golden.Rest.Types.IdInput do
  @moduledoc false

  alias BowlineClient.Read
  alias BowlineClient.Rules

  @type t :: %__MODULE__{
          id: integer()
        }

  defstruct [
    :id
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "IdInput")

    %__MODULE__{
      id: Read.int_value(Map.get(map, "id"), "id")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "id" => v.id
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{} = v) do
    List.flatten([
      Rules.required(["id"], v.id)
    ])
  end
end

defmodule Golden.Rest.Types.Invoice do
  @moduledoc false

  alias BowlineClient.Read

  @type t :: %__MODULE__{
          id: integer(),
          title: String.t()
        }

  defstruct [
    :id,
    :title
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Invoice")

    %__MODULE__{
      id: Read.int_value(Map.get(map, "id"), "id"),
      title: Read.string_value(Map.get(map, "title"), "title")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "id" => v.id,
      "title" => v.title
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Rest.Types.ListInput do
  @moduledoc false

  alias BowlineClient.Encode
  alias BowlineClient.Read
  alias BowlineClient.Rules

  @type t :: %__MODULE__{
          limit: integer(),
          status: String.t(),
          tags: [String.t()],
          deep: boolean()
        }

  defstruct [
    :limit,
    :status,
    :tags,
    :deep
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "ListInput")

    %__MODULE__{
      limit: Read.int_value(Map.get(map, "limit"), "limit"),
      status: Read.string_value(Map.get(map, "status"), "status"),
      tags: Read.list_value(Map.get(map, "tags"), "tags", &Read.string_value(&1, "tags")),
      deep: Read.bool_value(Map.get(map, "deep"), "deep")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "limit" => v.limit,
      "status" => v.status,
      "tags" => Encode.list(v.tags, &Function.identity/1),
      "deep" => v.deep
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{} = v) do
    List.flatten([
      Rules.min(["limit"], v.limit, 1),
      Rules.max(["limit"], v.limit, 100)
    ])
  end
end

defmodule Golden.Rest.Types.RemoveInput do
  @moduledoc false

  alias BowlineClient.Read
  alias BowlineClient.Rules

  @type t :: %__MODULE__{
          id: integer(),
          force: boolean()
        }

  defstruct [
    :id,
    :force
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "RemoveInput")

    %__MODULE__{
      id: Read.int_value(Map.get(map, "id"), "id"),
      force: Read.bool_value(Map.get(map, "force"), "force")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "id" => v.id,
      "force" => v.force
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{} = v) do
    List.flatten([
      Rules.required(["id"], v.id)
    ])
  end
end

defmodule Golden.Rest.Types.ReplaceInput do
  @moduledoc false

  alias BowlineClient.Read
  alias BowlineClient.Rules

  @type t :: %__MODULE__{
          id: integer(),
          title: String.t()
        }

  defstruct [
    :id,
    :title
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "ReplaceInput")

    %__MODULE__{
      id: Read.int_value(Map.get(map, "id"), "id"),
      title: Read.string_value(Map.get(map, "title"), "title")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "id" => v.id,
      "title" => v.title
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{} = v) do
    List.flatten([
      Rules.required(["id"], v.id),
      Rules.required(["title"], v.title)
    ])
  end
end

defmodule Golden.Rest.Invoices do
  @moduledoc "Procedures under invoices."

  alias BowlineClient.Transport
  alias Golden.Rest.Types

  @doc """
  get calls invoices.get.
  """
  @spec get(Transport.t(), Types.IdInput.t(), Transport.call_opts()) ::
          {:ok, Types.Invoice.t()} | {:error, BowlineClient.Error.t()}
  def get(transport, %Types.IdInput{} = input, opts \\ []) do
    Transport.call(
      transport,
      "invoices/#{URI.encode_www_form(to_string(input.id))}",
      :get,
      Map.drop(Types.IdInput.to_map(input), ["id"]),
      &Types.Invoice.from_map/1,
      opts
    )
  end

  @spec get!(Transport.t(), Types.IdInput.t(), Transport.call_opts()) :: Types.Invoice.t()
  def get!(transport, input, opts \\ []) do
    case get(transport, input, opts) do
      {:ok, value} -> value
      {:error, error} -> raise error
    end
  end

  @doc """
  list calls invoices.list.
  """
  @spec list(Transport.t(), Types.ListInput.t(), Transport.call_opts()) ::
          {:ok, Types.Invoice.t()} | {:error, BowlineClient.Error.t()}
  def list(transport, %Types.ListInput{} = input, opts \\ []) do
    Transport.call(
      transport,
      "invoices",
      :get,
      Types.ListInput.to_map(input),
      &Types.Invoice.from_map/1,
      opts
    )
  end

  @spec list!(Transport.t(), Types.ListInput.t(), Transport.call_opts()) :: Types.Invoice.t()
  def list!(transport, input, opts \\ []) do
    case list(transport, input, opts) do
      {:ok, value} -> value
      {:error, error} -> raise error
    end
  end

  @doc """
  remove calls invoices.remove.
  """
  @spec remove(Transport.t(), Types.RemoveInput.t(), Transport.call_opts()) ::
          {:ok, Types.Invoice.t()} | {:error, BowlineClient.Error.t()}
  def remove(transport, %Types.RemoveInput{} = input, opts \\ []) do
    Transport.call(
      transport,
      "invoices/#{URI.encode_www_form(to_string(input.id))}",
      :post,
      Map.drop(Types.RemoveInput.to_map(input), ["id"]),
      &Types.Invoice.from_map/1,
      opts
    )
  end

  @spec remove!(Transport.t(), Types.RemoveInput.t(), Transport.call_opts()) :: Types.Invoice.t()
  def remove!(transport, input, opts \\ []) do
    case remove(transport, input, opts) do
      {:ok, value} -> value
      {:error, error} -> raise error
    end
  end

  @doc """
  replace calls invoices.replace.
  """
  @spec replace(Transport.t(), Types.ReplaceInput.t(), Transport.call_opts()) ::
          {:ok, Types.Invoice.t()} | {:error, BowlineClient.Error.t()}
  def replace(transport, %Types.ReplaceInput{} = input, opts \\ []) do
    Transport.call(
      transport,
      "invoices/#{URI.encode_www_form(to_string(input.id))}",
      :post,
      Map.drop(Types.ReplaceInput.to_map(input), ["id"]),
      &Types.Invoice.from_map/1,
      opts
    )
  end

  @spec replace!(Transport.t(), Types.ReplaceInput.t(), Transport.call_opts()) ::
          Types.Invoice.t()
  def replace!(transport, input, opts \\ []) do
    case replace(transport, input, opts) do
      {:ok, value} -> value
      {:error, error} -> raise error
    end
  end
end

defmodule Golden.Rest.Client do
  @moduledoc """
  Entry point for the API: `new/2` builds the transport every module takes first.

  Mounted modules:
  - `Golden.Rest.Invoices`
  """

  alias BowlineClient.Transport

  @spec new(String.t(), keyword()) :: Transport.t()
  def new(base_url, opts \\ []) do
    Transport.new(Keyword.put(opts, :base_url, base_url))
  end
end
