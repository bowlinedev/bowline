# Code generated by bowline. DO NOT EDIT.

defmodule Golden.Subscriptions.Types.Change do
  @moduledoc false

  alias BowlineClient.Encode
  alias BowlineClient.Read

  @type t :: %__MODULE__{
          id: integer(),
          at: DateTime.t(),
          tags: [String.t()]
        }

  defstruct [
    :id,
    :at,
    :tags
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Change")

    %__MODULE__{
      id: Read.int_value(Map.get(map, "id"), "id"),
      at: Read.timestamp_value(Map.get(map, "at"), "at"),
      tags: Read.list_value(Map.get(map, "tags"), "tags", &Read.string_value(&1, "tags"))
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "id" => v.id,
      "at" => Encode.timestamp(v.at),
      "tags" => Encode.list(v.tags, &Function.identity/1)
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Subscriptions.Types.WatchInput do
  @moduledoc false

  alias BowlineClient.Read
  alias BowlineClient.Rules

  @type t :: %__MODULE__{
          limit: integer()
        }

  defstruct [
    :limit
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "WatchInput")

    %__MODULE__{
      limit: Read.int_value(Map.get(map, "limit"), "limit")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "limit" => v.limit
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{} = v) do
    List.flatten([
      Rules.min(["limit"], v.limit, 1),
      Rules.max(["limit"], v.limit, 100)
    ])
  end
end

defmodule Golden.Subscriptions.Types.Gone do
  @moduledoc """
  Gone is the details shape of the Gone error variant (NOT_FOUND).
  """

  alias BowlineClient.Read

  @type t :: %__MODULE__{
          id: integer()
        }

  defstruct [
    :id
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Gone")

    %__MODULE__{
      id: Read.int_value(Map.get(map, "id"), "id")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "id" => v.id
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Subscriptions.Client do
  @moduledoc """
  Entry point for the API: `new/2` builds the transport every module takes first.
  """

  alias BowlineClient.Transport
  alias Golden.Subscriptions.Types

  @spec new(String.t(), keyword()) :: Transport.t()
  def new(base_url, opts \\ []) do
    Transport.new(Keyword.put(opts, :base_url, base_url))
  end

  @doc """
  secret calls secret.
  """
  @spec secret(Transport.t(), Types.WatchInput.t(), Transport.call_opts()) ::
          {:ok, Enumerable.t()} | {:error, BowlineClient.Error.t()}
  def secret(transport, %Types.WatchInput{} = input, opts \\ []) do
    Transport.subscribe(
      transport,
      "secret",
      Types.WatchInput.to_map(input),
      &Types.Change.from_map/1,
      opts
    )
  end

  @doc """
  Watch streams every change.
  """
  @spec watch(Transport.t(), Types.WatchInput.t(), Transport.call_opts()) ::
          {:ok, Enumerable.t()} | {:error, BowlineClient.Error.t()}
  def watch(transport, %Types.WatchInput{} = input, opts \\ []) do
    Transport.subscribe(
      transport,
      "watch",
      Types.WatchInput.to_map(input),
      &Types.Change.from_map/1,
      opts
    )
  end

  @doc "Decodes the details of a declared error variant into its struct; nil for other errors."
  @spec error_details(BowlineClient.Error.t()) :: struct() | nil
  def error_details(%BowlineClient.Error{type: "Gone", details: details})
      when is_map(details) do
    Types.Gone.from_map(details)
  end

  def error_details(%BowlineClient.Error{}), do: nil
end
