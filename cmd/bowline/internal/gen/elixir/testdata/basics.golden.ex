# Code generated by bowline. DO NOT EDIT.

defmodule Golden.Basics.Types.Input do
  @moduledoc false

  alias BowlineClient.Read

  @type t :: %__MODULE__{
          name: String.t(),
          count: integer(),
          small: integer(),
          big: integer(),
          ratio: float(),
          on: boolean()
        }

  defstruct [
    :name,
    :count,
    :small,
    :big,
    :ratio,
    :on
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Input")

    %__MODULE__{
      name: Read.string_value(Map.get(map, "name"), "name"),
      count: Read.int_value(Map.get(map, "count"), "count"),
      small: Read.int_value(Map.get(map, "small"), "small"),
      big: Read.int_value(Map.get(map, "big"), "big"),
      ratio: Read.float_value(Map.get(map, "ratio"), "ratio"),
      on: Read.bool_value(Map.get(map, "on"), "on")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "name" => v.name,
      "count" => v.count,
      "small" => v.small,
      "big" => v.big,
      "ratio" => v.ratio,
      "on" => v.on
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Basics.Types.Output do
  @moduledoc false

  alias BowlineClient.Read

  @type t :: %__MODULE__{
          echo: String.t()
        }

  defstruct [
    :echo
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Output")

    %__MODULE__{
      echo: Read.string_value(Map.get(map, "echo"), "echo")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "echo" => v.echo
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Basics.Client do
  @moduledoc """
  Entry point for the API: `new/2` builds the transport every module takes first.
  """

  alias BowlineClient.Transport
  alias Golden.Basics.Types

  @spec new(String.t(), keyword()) :: Transport.t()
  def new(base_url, opts \\ []) do
    Transport.new(Keyword.put(opts, :base_url, base_url))
  end

  @doc """
  echo calls echo.
  """
  @spec echo(Transport.t(), Types.Input.t(), Transport.call_opts()) ::
          {:ok, Types.Output.t()} | {:error, BowlineClient.Error.t()}
  def echo(transport, %Types.Input{} = input, opts \\ []) do
    Transport.call(
      transport,
      "echo",
      :get,
      Types.Input.to_map(input),
      &Types.Output.from_map/1,
      opts
    )
  end

  @spec echo!(Transport.t(), Types.Input.t(), Transport.call_opts()) :: Types.Output.t()
  def echo!(transport, input, opts \\ []) do
    case echo(transport, input, opts) do
      {:ok, value} -> value
      {:error, error} -> raise error
    end
  end
end
