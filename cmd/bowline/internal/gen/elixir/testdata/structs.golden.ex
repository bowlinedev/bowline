# Code generated by bowline. DO NOT EDIT.

defmodule Golden.Structs.Types.Address do
  @moduledoc """
  Address is where mail goes.
  """

  alias BowlineClient.Read
  alias BowlineClient.Rules

  @type t :: %__MODULE__{
          street: String.t(),
          city: String.t()
        }

  defstruct [
    :street,
    :city
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Address")

    %__MODULE__{
      street: Read.string_value(Map.get(map, "street"), "street"),
      city: Read.string_value(Map.get(map, "city"), "city")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "street" => v.street,
      "city" => v.city
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{} = v) do
    List.flatten([
      Rules.required(["city"], v.city)
    ])
  end
end

defmodule Golden.Structs.Types.Person.Inline do
  @moduledoc false

  alias BowlineClient.Read

  @type t :: %__MODULE__{
          x: integer()
        }

  defstruct [
    :x
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Inline")

    %__MODULE__{
      x: Read.int_value(Map.get(map, "x"), "x")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "x" => v.x
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Structs.Types.Person do
  @moduledoc false

  alias BowlineClient.Read
  alias BowlineClient.Rules
  alias Golden.Structs.Types

  @type t :: %__MODULE__{
          name: String.t(),
          age: integer(),
          email: String.t(),
          home: Types.Address.t(),
          no_tag: String.t(),
          inline: Golden.Structs.Types.Person.Inline.t()
        }

  defstruct [
    :name,
    :age,
    :email,
    :home,
    :no_tag,
    :inline
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Person")

    %__MODULE__{
      name: Read.string_value(Map.get(map, "name"), "name"),
      age: Read.int_value(Map.get(map, "age"), "age"),
      email: Read.string_value(Map.get(map, "email"), "email"),
      home: Types.Address.from_map(Map.get(map, "home")),
      no_tag: Read.string_value(Map.get(map, "NoTag"), "NoTag"),
      inline: Golden.Structs.Types.Person.Inline.from_map(Map.get(map, "inline"))
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "name" => v.name,
      "age" => v.age,
      "email" => v.email,
      "home" => Types.Address.to_map(v.home),
      "NoTag" => v.no_tag,
      "inline" => Golden.Structs.Types.Person.Inline.to_map(v.inline)
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{} = v) do
    List.flatten([
      Rules.required(["name"], v.name),
      Rules.min(["name"], v.name, 1),
      Rules.max(["name"], v.name, 80),
      Rules.min(["age"], v.age, 0),
      Rules.max(["age"], v.age, 150),
      Rules.email(["email"], v.email),
      Read.prefixed(["home"], Types.Address.validate(v.home)),
      Read.prefixed(["inline"], Golden.Structs.Types.Person.Inline.validate(v.inline))
    ])
  end
end

defmodule Golden.Structs.Client do
  @moduledoc """
  Entry point for the API: `new/2` builds the transport every module takes first.
  """

  alias BowlineClient.Transport
  alias Golden.Structs.Types

  @spec new(String.t(), keyword()) :: Transport.t()
  def new(base_url, opts \\ []) do
    Transport.new(Keyword.put(opts, :base_url, base_url))
  end

  @doc """
  get calls get.
  """
  @spec get(Transport.t(), Types.Address.t(), Transport.call_opts()) ::
          {:ok, Types.Person.t()} | {:error, BowlineClient.Error.t()}
  def get(transport, %Types.Address{} = input, opts \\ []) do
    Transport.call(
      transport,
      "get",
      :get,
      Types.Address.to_map(input),
      &Types.Person.from_map/1,
      opts
    )
  end

  @spec get!(Transport.t(), Types.Address.t(), Transport.call_opts()) :: Types.Person.t()
  def get!(transport, input, opts \\ []) do
    case get(transport, input, opts) do
      {:ok, value} -> value
      {:error, error} -> raise error
    end
  end
end
