# Code generated by bowline. DO NOT EDIT.

defmodule Golden.Paths.Types.CreateInput do
  @moduledoc false

  alias BowlineClient.Read
  alias BowlineClient.Rules

  @type t :: %__MODULE__{
          total: String.t()
        }

  defstruct [
    :total
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "CreateInput")

    %__MODULE__{
      total: Read.string_value(Map.get(map, "total"), "total")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "total" => v.total
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{} = v) do
    List.flatten([
      Rules.required(["total"], v.total)
    ])
  end
end

defmodule Golden.Paths.Types.GetInput do
  @moduledoc false

  alias BowlineClient.Read
  alias BowlineClient.Rules

  @type t :: %__MODULE__{
          id: integer()
        }

  defstruct [
    :id
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "GetInput")

    %__MODULE__{
      id: Read.int_value(Map.get(map, "id"), "id")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "id" => v.id
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{} = v) do
    List.flatten([
      Rules.required(["id"], v.id)
    ])
  end
end

defmodule Golden.Paths.Types.Invoice do
  @moduledoc false

  alias BowlineClient.Read

  @type t :: %__MODULE__{
          id: integer(),
          total: String.t()
        }

  defstruct [
    :id,
    :total
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Invoice")

    %__MODULE__{
      id: Read.int_value(Map.get(map, "id"), "id"),
      total: Read.string_value(Map.get(map, "total"), "total")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "id" => v.id,
      "total" => v.total
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Paths.Types.LineInput do
  @moduledoc false

  alias BowlineClient.Read
  alias BowlineClient.Rules

  @type t :: %__MODULE__{
          invoice_id: integer(),
          line_id: String.t()
        }

  defstruct [
    :invoice_id,
    :line_id
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "LineInput")

    %__MODULE__{
      invoice_id: Read.int_value(Map.get(map, "invoiceId"), "invoiceId"),
      line_id: Read.string_value(Map.get(map, "lineId"), "lineId")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "invoiceId" => v.invoice_id,
      "lineId" => v.line_id
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{} = v) do
    List.flatten([
      Rules.required(["invoiceId"], v.invoice_id),
      Rules.required(["lineId"], v.line_id)
    ])
  end
end

defmodule Golden.Paths.Types.ListInput do
  @moduledoc false

  alias BowlineClient.Read

  @type t :: %__MODULE__{
          limit: integer()
        }

  defstruct [
    :limit
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "ListInput")

    %__MODULE__{
      limit: Read.int_value(Map.get(map, "limit"), "limit")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "limit" => v.limit
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Paths.Invoices do
  @moduledoc "Procedures under invoices."

  alias BowlineClient.Transport
  alias Golden.Paths.Types

  @doc """
  create calls invoices.create.
  """
  @spec create(Transport.t(), Types.CreateInput.t(), Transport.call_opts()) ::
          {:ok, Types.Invoice.t()} | {:error, BowlineClient.Error.t()}
  def create(transport, %Types.CreateInput{} = input, opts \\ []) do
    Transport.call(
      transport,
      "invoices",
      :post,
      Types.CreateInput.to_map(input),
      &Types.Invoice.from_map/1,
      opts
    )
  end

  @spec create!(Transport.t(), Types.CreateInput.t(), Transport.call_opts()) :: Types.Invoice.t()
  def create!(transport, input, opts \\ []) do
    case create(transport, input, opts) do
      {:ok, value} -> value
      {:error, error} -> raise error
    end
  end

  @doc """
  get calls invoices.get.
  """
  @spec get(Transport.t(), Types.GetInput.t(), Transport.call_opts()) ::
          {:ok, Types.Invoice.t()} | {:error, BowlineClient.Error.t()}
  def get(transport, %Types.GetInput{} = input, opts \\ []) do
    Transport.rest(
      transport,
      "invoices/#{URI.encode_www_form(to_string(input.id))}",
      :get,
      Map.drop(Types.GetInput.to_map(input), ["id"]),
      &Types.Invoice.from_map/1,
      opts
    )
  end

  @spec get!(Transport.t(), Types.GetInput.t(), Transport.call_opts()) :: Types.Invoice.t()
  def get!(transport, input, opts \\ []) do
    case get(transport, input, opts) do
      {:ok, value} -> value
      {:error, error} -> raise error
    end
  end

  @doc """
  line calls invoices.line.
  """
  @spec line(Transport.t(), Types.LineInput.t(), Transport.call_opts()) ::
          {:ok, Types.Invoice.t()} | {:error, BowlineClient.Error.t()}
  def line(transport, %Types.LineInput{} = input, opts \\ []) do
    Transport.rest(
      transport,
      "invoices/#{URI.encode_www_form(to_string(input.invoice_id))}/lines/#{URI.encode_www_form(to_string(input.line_id))}",
      :get,
      Map.drop(Types.LineInput.to_map(input), ["invoiceId", "lineId"]),
      &Types.Invoice.from_map/1,
      opts
    )
  end

  @spec line!(Transport.t(), Types.LineInput.t(), Transport.call_opts()) :: Types.Invoice.t()
  def line!(transport, input, opts \\ []) do
    case line(transport, input, opts) do
      {:ok, value} -> value
      {:error, error} -> raise error
    end
  end

  @doc """
  list calls invoices.list.
  """
  @spec list(Transport.t(), Types.ListInput.t(), Transport.call_opts()) ::
          {:ok, Types.Invoice.t()} | {:error, BowlineClient.Error.t()}
  def list(transport, %Types.ListInput{} = input, opts \\ []) do
    Transport.rest(
      transport,
      "invoices",
      :get,
      Types.ListInput.to_map(input),
      &Types.Invoice.from_map/1,
      opts
    )
  end

  @spec list!(Transport.t(), Types.ListInput.t(), Transport.call_opts()) :: Types.Invoice.t()
  def list!(transport, input, opts \\ []) do
    case list(transport, input, opts) do
      {:ok, value} -> value
      {:error, error} -> raise error
    end
  end
end

defmodule Golden.Paths.Client do
  @moduledoc """
  Entry point for the API: `new/2` builds the transport every module takes first.

  Mounted modules:
  - `Golden.Paths.Invoices`
  """

  alias BowlineClient.Transport

  @spec new(String.t(), keyword()) :: Transport.t()
  def new(base_url, opts \\ []) do
    Transport.new(Keyword.put(opts, :base_url, base_url))
  end
end
