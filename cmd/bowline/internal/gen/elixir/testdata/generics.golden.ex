# Code generated by bowline. DO NOT EDIT.

defmodule Golden.Generics.Types.Page do
  @moduledoc """
  Page is one page of results.
  """

  alias BowlineClient.Encode
  alias BowlineClient.Read

  @type t(param_t) :: %__MODULE__{
          items: [param_t],
          next: String.t() | nil
        }

  defstruct [
    :items,
    :next
  ]

  @spec from_map(term(), (term() -> term())) :: t(term())
  def from_map(json, decode_t) do
    map = Read.object(json, "Page")

    %__MODULE__{
      items: Read.list_value(Map.get(map, "items"), "items", decode_t),
      next: Read.optional_value(Map.get(map, "next"), &Read.string_value(&1, "next"))
    }
  end

  @spec to_map(t(term()), (term() -> term())) :: map()
  def to_map(%__MODULE__{} = v, encode_t) do
    %{
      "items" => Encode.list(v.items, encode_t)
    }
    |> Encode.optional("next", v.next, &Function.identity/1)
  end

  @spec validate(t(term()), (term() -> [BowlineClient.Issue.t()])) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{} = v, validate_t) do
    List.flatten([
      Read.validate_list(["items"], v.items, validate_t)
    ])
  end
end

defmodule Golden.Generics.Types.Pair do
  @moduledoc false

  alias BowlineClient.Read

  @type t(param_k, param_v) :: %__MODULE__{
          key: param_k,
          value: param_v
        }

  defstruct [
    :key,
    :value
  ]

  @spec from_map(term(), (term() -> term()), (term() -> term())) :: t(term(), term())
  def from_map(json, decode_k, decode_v) do
    map = Read.object(json, "Pair")

    %__MODULE__{
      key: decode_k.(Map.get(map, "key")),
      value: decode_v.(Map.get(map, "value"))
    }
  end

  @spec to_map(t(term(), term()), (term() -> term()), (term() -> term())) :: map()
  def to_map(%__MODULE__{} = v, encode_k, encode_v) do
    %{
      "key" => encode_k.(v.key),
      "value" => encode_v.(v.value)
    }
  end

  @spec validate(
          t(term(), term()),
          (term() -> [BowlineClient.Issue.t()]),
          (term() -> [BowlineClient.Issue.t()])
        ) ::
          [BowlineClient.Issue.t()]
  def validate(%__MODULE__{} = v, validate_k, validate_v) do
    List.flatten([
      Read.prefixed(["key"], validate_k.(v.key)),
      Read.prefixed(["value"], validate_v.(v.value))
    ])
  end
end

defmodule Golden.Generics.Types.RangeFloat64 do
  @moduledoc """
  RangeFloat64 is an instantiation of fidelity.test/rows/generics.Range.
  """

  alias BowlineClient.Read

  @type t :: %__MODULE__{
          low: float(),
          high: float()
        }

  defstruct [
    :low,
    :high
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "RangeFloat64")

    %__MODULE__{
      low: Read.float_value(Map.get(map, "low"), "low"),
      high: Read.float_value(Map.get(map, "high"), "high")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "low" => v.low,
      "high" => v.high
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Generics.Types.RangeInt32 do
  @moduledoc """
  RangeInt32 is an instantiation of fidelity.test/rows/generics.Range.
  """

  alias BowlineClient.Read

  @type t :: %__MODULE__{
          low: integer(),
          high: integer()
        }

  defstruct [
    :low,
    :high
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "RangeInt32")

    %__MODULE__{
      low: Read.int_value(Map.get(map, "low"), "low"),
      high: Read.int_value(Map.get(map, "high"), "high")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "low" => v.low,
      "high" => v.high
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Generics.Types.RangeInt64 do
  @moduledoc """
  RangeInt64 is an instantiation of fidelity.test/rows/generics.Range.
  """

  alias BowlineClient.Read

  @type t :: %__MODULE__{
          low: integer(),
          high: integer()
        }

  defstruct [
    :low,
    :high
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "RangeInt64")

    %__MODULE__{
      low: Read.int_value(Map.get(map, "low"), "low"),
      high: Read.int_value(Map.get(map, "high"), "high")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "low" => v.low,
      "high" => v.high
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Generics.Types.Report do
  @moduledoc false

  alias BowlineClient.Encode
  alias BowlineClient.Read
  alias Golden.Generics.Types

  @type t :: %__MODULE__{
          users: Types.Page.t(Types.User.t()),
          nested: Types.Page.t(Types.Page.t(Types.User.t())),
          pairs: [Types.Pair.t(String.t(), Types.User.t())],
          tree: Types.Tree.t(Types.User.t()),
          ints: Types.RangeInt64.t(),
          floats: Types.RangeFloat64.t(),
          scores: Types.Page.t(Types.RangeInt32.t())
        }

  defstruct [
    :users,
    :nested,
    :pairs,
    :tree,
    :ints,
    :floats,
    :scores
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Report")

    %__MODULE__{
      users: Types.Page.from_map(Map.get(map, "users"), &Types.User.from_map/1),
      nested: Types.Page.from_map(Map.get(map, "nested"), &decode_nested/1),
      pairs: Read.list_value(Map.get(map, "pairs"), "pairs", &decode_pairs/1),
      tree: Types.Tree.from_map(Map.get(map, "tree"), &Types.User.from_map/1),
      ints: Types.RangeInt64.from_map(Map.get(map, "ints")),
      floats: Types.RangeFloat64.from_map(Map.get(map, "floats")),
      scores: Types.Page.from_map(Map.get(map, "scores"), &Types.RangeInt32.from_map/1)
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "users" => Types.Page.to_map(v.users, &Types.User.to_map/1),
      "nested" => Types.Page.to_map(v.nested, &encode_nested/1),
      "pairs" => Encode.list(v.pairs, &encode_pairs/1),
      "tree" => Types.Tree.to_map(v.tree, &Types.User.to_map/1),
      "ints" => Types.RangeInt64.to_map(v.ints),
      "floats" => Types.RangeFloat64.to_map(v.floats),
      "scores" => Types.Page.to_map(v.scores, &Types.RangeInt32.to_map/1)
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{} = v) do
    List.flatten([
      Read.prefixed(["users"], Types.Page.validate(v.users, &Types.User.validate/1)),
      Read.prefixed(["nested"], Types.Page.validate(v.nested, &validate_nested/1)),
      Read.validate_list(["pairs"], v.pairs, &validate_pairs/1),
      Read.prefixed(["tree"], Types.Tree.validate(v.tree, &Types.User.validate/1)),
      Read.prefixed(["ints"], Types.RangeInt64.validate(v.ints)),
      Read.prefixed(["floats"], Types.RangeFloat64.validate(v.floats)),
      Read.prefixed(["scores"], Types.Page.validate(v.scores, &Types.RangeInt32.validate/1))
    ])
  end

  defp decode_nested(value) do
    Types.Page.from_map(value, &Types.User.from_map/1)
  end

  defp decode_pairs(value) do
    Types.Pair.from_map(value, &Read.string_value(&1, "pairs"), &Types.User.from_map/1)
  end

  defp encode_nested(value) do
    Types.Page.to_map(value, &Types.User.to_map/1)
  end

  defp encode_pairs(value) do
    Types.Pair.to_map(value, &Function.identity/1, &Types.User.to_map/1)
  end

  defp validate_nested(value) do
    Types.Page.validate(value, &Types.User.validate/1)
  end

  defp validate_pairs(value) do
    Types.Pair.validate(value, fn _ -> [] end, &Types.User.validate/1)
  end
end

defmodule Golden.Generics.Types.Tree do
  @moduledoc false

  alias BowlineClient.Encode
  alias BowlineClient.Read
  alias Golden.Generics.Types

  @type t(param_t) :: %__MODULE__{
          value: param_t,
          children: [Types.Tree.t(param_t)]
        }

  defstruct [
    :value,
    :children
  ]

  @spec from_map(term(), (term() -> term())) :: t(term())
  def from_map(json, decode_t) do
    map = Read.object(json, "Tree")

    %__MODULE__{
      value: decode_t.(Map.get(map, "value")),
      children: field_children(map, decode_t)
    }
  end

  @spec to_map(t(term()), (term() -> term())) :: map()
  def to_map(%__MODULE__{} = v, encode_t) do
    %{
      "value" => encode_t.(v.value),
      "children" => Encode.list(v.children, &encode_children(&1, encode_t))
    }
  end

  @spec validate(t(term()), (term() -> [BowlineClient.Issue.t()])) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{} = v, validate_t) do
    List.flatten([
      Read.prefixed(["value"], validate_t.(v.value)),
      Read.validate_list(["children"], v.children, &validate_children(&1, validate_t))
    ])
  end

  defp decode_children(value, decode_t) do
    Types.Tree.from_map(value, decode_t)
  end

  defp field_children(map, decode_t) do
    Read.list_value(Map.get(map, "children"), "children", &decode_children(&1, decode_t))
  end

  defp encode_children(value, encode_t) do
    Types.Tree.to_map(value, encode_t)
  end

  defp validate_children(value, validate_t) do
    Types.Tree.validate(value, validate_t)
  end
end

defmodule Golden.Generics.Types.User do
  @moduledoc false

  alias BowlineClient.Read

  @type t :: %__MODULE__{
          name: String.t()
        }

  defstruct [
    :name
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "User")

    %__MODULE__{
      name: Read.string_value(Map.get(map, "name"), "name")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "name" => v.name
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Generics.Client do
  @moduledoc """
  Entry point for the API: `new/2` builds the transport every module takes first.
  """

  alias BowlineClient.Transport
  alias Golden.Generics.Types

  @spec new(String.t(), keyword()) :: Transport.t()
  def new(base_url, opts \\ []) do
    Transport.new(Keyword.put(opts, :base_url, base_url))
  end

  @doc """
  get calls get.
  """
  @spec get(Transport.t(), Types.Page.t(Types.User.t()), Transport.call_opts()) ::
          {:ok, Types.Report.t()} | {:error, BowlineClient.Error.t()}
  def get(transport, input, opts \\ []) do
    Transport.call(
      transport,
      "get",
      :get,
      Types.Page.to_map(input, &Types.User.to_map/1),
      &Types.Report.from_map/1,
      opts
    )
  end

  @spec get!(Transport.t(), Types.Page.t(Types.User.t()), Transport.call_opts()) ::
          Types.Report.t()
  def get!(transport, input, opts \\ []) do
    case get(transport, input, opts) do
      {:ok, value} -> value
      {:error, error} -> raise error
    end
  end
end
