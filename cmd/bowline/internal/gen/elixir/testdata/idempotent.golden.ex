# Code generated by bowline. DO NOT EDIT.

defmodule Golden.Idempotent.Types.CreateInput do
  @moduledoc false

  alias BowlineClient.Read
  alias BowlineClient.Rules

  @type t :: %__MODULE__{
          name: String.t()
        }

  defstruct [
    :name
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "CreateInput")

    %__MODULE__{
      name: Read.string_value(Map.get(map, "name"), "name")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "name" => v.name
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{} = v) do
    List.flatten([
      Rules.required(["name"], v.name)
    ])
  end
end

defmodule Golden.Idempotent.Types.Created do
  @moduledoc false

  alias BowlineClient.Read

  @type t :: %__MODULE__{
          id: integer()
        }

  defstruct [
    :id
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Created")

    %__MODULE__{
      id: Read.int_value(Map.get(map, "id"), "id")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "id" => v.id
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Idempotent.Client do
  @moduledoc """
  Entry point for the API: `new/2` builds the transport every module takes first.
  """

  alias BowlineClient.Transport
  alias Golden.Idempotent.Types

  @spec new(String.t(), keyword()) :: Transport.t()
  def new(base_url, opts \\ []) do
    Transport.new(Keyword.put(opts, :base_url, base_url))
  end

  @doc """
  create calls create.
  """
  @spec create(Transport.t(), Types.CreateInput.t(), Transport.call_opts()) ::
          {:ok, Types.Created.t()} | {:error, BowlineClient.Error.t()}
  def create(transport, %Types.CreateInput{} = input, opts \\ []) do
    Transport.call(
      transport,
      "create",
      :post,
      Types.CreateInput.to_map(input),
      &Types.Created.from_map/1,
      opts
    )
  end

  @spec create!(Transport.t(), Types.CreateInput.t(), Transport.call_opts()) :: Types.Created.t()
  def create!(transport, input, opts \\ []) do
    case create(transport, input, opts) do
      {:ok, value} -> value
      {:error, error} -> raise error
    end
  end
end
