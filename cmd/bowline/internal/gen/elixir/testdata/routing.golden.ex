# Code generated by bowline. DO NOT EDIT.

defmodule Golden.Routing.Types.Item do
  @moduledoc false

  alias BowlineClient.Read

  @type t :: %__MODULE__{
          name: String.t()
        }

  defstruct [
    :name
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Item")

    %__MODULE__{
      name: Read.string_value(Map.get(map, "name"), "name")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "name" => v.name
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Routing.Types.RoutingId do
  @moduledoc false

  alias BowlineClient.Read

  @type t :: %__MODULE__{
          id: integer()
        }

  defstruct [
    :id
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "RoutingId")

    %__MODULE__{
      id: Read.int_value(Map.get(map, "id"), "id")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "id" => v.id
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Routing.Types.SubId do
  @moduledoc false

  alias BowlineClient.Read

  @type t :: %__MODULE__{
          id: integer()
        }

  defstruct [
    :id
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "SubId")

    %__MODULE__{
      id: Read.int_value(Map.get(map, "id"), "id")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "id" => v.id
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Routing.Admin do
  @moduledoc "Procedures under admin."

  alias BowlineClient.Transport
  alias Golden.Routing.Types

  @doc """
  purge calls admin.purge.

  Deprecated: use sub.remove
  """
  @spec purge(Transport.t(), Types.RoutingId.t(), Transport.call_opts()) ::
          {:ok, BowlineClient.Empty.t()} | {:error, BowlineClient.Error.t()}
  def purge(transport, %Types.RoutingId{} = input, opts \\ []) do
    Transport.call(
      transport,
      "admin.purge",
      :post,
      Types.RoutingId.to_map(input),
      &BowlineClient.Empty.from_map/1,
      opts
    )
  end

  @spec purge!(Transport.t(), Types.RoutingId.t(), Transport.call_opts()) ::
          BowlineClient.Empty.t()
  def purge!(transport, input, opts \\ []) do
    case purge(transport, input, opts) do
      {:ok, value} -> value
      {:error, error} -> raise error
    end
  end
end

defmodule Golden.Routing.Sub do
  @moduledoc "Procedures under sub."

  alias BowlineClient.Transport
  alias Golden.Routing.Types

  @doc """
  Remove deletes an item by ID.
  """
  @spec remove(Transport.t(), Types.SubId.t(), Transport.call_opts()) ::
          {:ok, BowlineClient.Empty.t()} | {:error, BowlineClient.Error.t()}
  def remove(transport, %Types.SubId{} = input, opts \\ []) do
    Transport.call(
      transport,
      "sub.remove",
      :post,
      Types.SubId.to_map(input),
      &BowlineClient.Empty.from_map/1,
      opts
    )
  end

  @spec remove!(Transport.t(), Types.SubId.t(), Transport.call_opts()) :: BowlineClient.Empty.t()
  def remove!(transport, input, opts \\ []) do
    case remove(transport, input, opts) do
      {:ok, value} -> value
      {:error, error} -> raise error
    end
  end
end

defmodule Golden.Routing.Client do
  @moduledoc """
  Entry point for the API: `new/2` builds the transport every module takes first.

  Mounted modules:
  - `Golden.Routing.Admin`
  - `Golden.Routing.Sub`
  """

  alias BowlineClient.Transport
  alias Golden.Routing.Types

  @spec new(String.t(), keyword()) :: Transport.t()
  def new(base_url, opts \\ []) do
    Transport.new(Keyword.put(opts, :base_url, base_url))
  end

  @doc """
  Get fetches an item.
  """
  @spec get(Transport.t(), Types.RoutingId.t(), Transport.call_opts()) ::
          {:ok, Types.Item.t()} | {:error, BowlineClient.Error.t()}
  def get(transport, %Types.RoutingId{} = input, opts \\ []) do
    Transport.call(
      transport,
      "get",
      :get,
      Types.RoutingId.to_map(input),
      &Types.Item.from_map/1,
      opts
    )
  end

  @spec get!(Transport.t(), Types.RoutingId.t(), Transport.call_opts()) :: Types.Item.t()
  def get!(transport, input, opts \\ []) do
    case get(transport, input, opts) do
      {:ok, value} -> value
      {:error, error} -> raise error
    end
  end

  @doc """
  search calls search.
  """
  @spec search(Transport.t(), Types.Item.t(), Transport.call_opts()) ::
          {:ok, Types.Item.t()} | {:error, BowlineClient.Error.t()}
  def search(transport, %Types.Item{} = input, opts \\ []) do
    Transport.call(
      transport,
      "search",
      :post,
      Types.Item.to_map(input),
      &Types.Item.from_map/1,
      opts
    )
  end

  @spec search!(Transport.t(), Types.Item.t(), Transport.call_opts()) :: Types.Item.t()
  def search!(transport, input, opts \\ []) do
    case search(transport, input, opts) do
      {:ok, value} -> value
      {:error, error} -> raise error
    end
  end
end
