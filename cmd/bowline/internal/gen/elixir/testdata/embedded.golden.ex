# Code generated by bowline. DO NOT EDIT.

defmodule Golden.Embedded.Types.Audit do
  @moduledoc false

  alias BowlineClient.Read

  @type t :: %__MODULE__{
          created_by: String.t(),
          version: integer()
        }

  defstruct [
    :created_by,
    :version
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Audit")

    %__MODULE__{
      created_by: Read.string_value(Map.get(map, "createdBy"), "createdBy"),
      version: Read.int_value(Map.get(map, "version"), "version")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "createdBy" => v.created_by,
      "version" => v.version
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Embedded.Types.Named do
  @moduledoc false

  alias BowlineClient.Read

  @type t :: %__MODULE__{
          name: String.t()
        }

  defstruct [
    :name
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Named")

    %__MODULE__{
      name: Read.string_value(Map.get(map, "name"), "name")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "name" => v.name
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Embedded.Types.Record do
  @moduledoc false

  alias BowlineClient.Encode
  alias BowlineClient.Read
  alias Golden.Embedded.Types

  @type t :: %__MODULE__{
          id: integer(),
          created_by: String.t(),
          version: integer(),
          named: Types.Named.t() | nil,
          extra: String.t()
        }

  defstruct [
    :id,
    :created_by,
    :version,
    :named,
    :extra
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Record")

    %__MODULE__{
      id: Read.int_value(Map.get(map, "id"), "id"),
      created_by: Read.string_value(Map.get(map, "createdBy"), "createdBy"),
      version: Read.int_value(Map.get(map, "version"), "version"),
      named: Read.optional_value(Map.get(map, "named"), &Types.Named.from_map/1),
      extra: Read.string_value(Map.get(map, "extra"), "extra")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "id" => v.id,
      "createdBy" => v.created_by,
      "version" => v.version,
      "named" => Encode.nullable(v.named, &Types.Named.to_map/1),
      "extra" => v.extra
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{} = v) do
    List.flatten([
      Read.validate_optional(v.named, &validate_named/1)
    ])
  end

  defp validate_named(value) do
    Read.prefixed(["named"], Types.Named.validate(value))
  end
end

defmodule Golden.Embedded.Client do
  @moduledoc """
  Entry point for the API: `new/2` builds the transport every module takes first.
  """

  alias BowlineClient.Transport
  alias Golden.Embedded.Types

  @spec new(String.t(), keyword()) :: Transport.t()
  def new(base_url, opts \\ []) do
    Transport.new(Keyword.put(opts, :base_url, base_url))
  end

  @doc """
  get calls get.
  """
  @spec get(Transport.t(), Types.Audit.t(), Transport.call_opts()) ::
          {:ok, Types.Record.t()} | {:error, BowlineClient.Error.t()}
  def get(transport, %Types.Audit{} = input, opts \\ []) do
    Transport.call(
      transport,
      "get",
      :get,
      Types.Audit.to_map(input),
      &Types.Record.from_map/1,
      opts
    )
  end

  @spec get!(Transport.t(), Types.Audit.t(), Transport.call_opts()) :: Types.Record.t()
  def get!(transport, input, opts \\ []) do
    case get(transport, input, opts) do
      {:ok, value} -> value
      {:error, error} -> raise error
    end
  end
end
