# Code generated by bowline. DO NOT EDIT.

defmodule Golden.Pointers.Types.Doc do
  @moduledoc false

  alias BowlineClient.Encode
  alias BowlineClient.Read
  alias Golden.Pointers.Types

  @type t :: %__MODULE__{
          title: String.t() | nil,
          subtitle: String.t() | nil,
          primary: Types.Tag.t() | nil,
          tags: [Types.Tag.t() | nil],
          by_name: %{optional(String.t()) => Types.Tag.t() | nil},
          count: integer() | nil
        }

  defstruct [
    :title,
    :subtitle,
    :primary,
    :tags,
    :by_name,
    :count
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Doc")

    %__MODULE__{
      title: Read.optional_value(Map.get(map, "title"), &Read.string_value(&1, "title")),
      subtitle: Read.optional_value(Map.get(map, "subtitle"), &Read.string_value(&1, "subtitle")),
      primary: Read.optional_value(Map.get(map, "primary"), &Types.Tag.from_map/1),
      tags: Read.list_value(Map.get(map, "tags"), "tags", &decode_tags/1),
      by_name: Read.map_value(Map.get(map, "byName"), "byName", &decode_by_name/1),
      count: Read.optional_value(Map.get(map, "count"), &Read.int_value(&1, "count"))
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "title" => Encode.nullable(v.title, &Function.identity/1),
      "primary" => Encode.nullable(v.primary, &Types.Tag.to_map/1),
      "tags" => Encode.list(v.tags, &encode_tags/1),
      "byName" => Encode.map(v.by_name, &encode_by_name/1)
    }
    |> Encode.optional("subtitle", v.subtitle, &Function.identity/1)
    |> Encode.optional("count", v.count, &Function.identity/1)
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{} = v) do
    List.flatten([
      Read.validate_optional(v.primary, &validate_primary/1),
      Read.validate_list(["tags"], v.tags, &validate_tags/1),
      Read.validate_map(["byName"], v.by_name, &validate_by_name/1)
    ])
  end

  defp decode_tags(value) do
    Read.optional_value(value, &Types.Tag.from_map/1)
  end

  defp decode_by_name(value) do
    Read.optional_value(value, &Types.Tag.from_map/1)
  end

  defp encode_tags(value) do
    Encode.nullable(value, &Types.Tag.to_map/1)
  end

  defp encode_by_name(value) do
    Encode.nullable(value, &Types.Tag.to_map/1)
  end

  defp validate_primary(value) do
    Read.prefixed(["primary"], Types.Tag.validate(value))
  end

  defp validate_tags(value) do
    Read.validate_optional(value, &Types.Tag.validate/1)
  end

  defp validate_by_name(value) do
    Read.validate_optional(value, &Types.Tag.validate/1)
  end
end

defmodule Golden.Pointers.Types.Tag do
  @moduledoc false

  alias BowlineClient.Read

  @type t :: %__MODULE__{
          label: String.t()
        }

  defstruct [
    :label
  ]

  @spec from_map(term()) :: t()
  def from_map(json) do
    map = Read.object(json, "Tag")

    %__MODULE__{
      label: Read.string_value(Map.get(map, "label"), "label")
    }
  end

  @spec to_map(t()) :: map()
  def to_map(%__MODULE__{} = v) do
    %{
      "label" => v.label
    }
  end

  @spec validate(t()) :: [BowlineClient.Issue.t()]
  def validate(%__MODULE__{}), do: []
end

defmodule Golden.Pointers.Client do
  @moduledoc """
  Entry point for the API: `new/2` builds the transport every module takes first.
  """

  alias BowlineClient.Transport
  alias Golden.Pointers.Types

  @spec new(String.t(), keyword()) :: Transport.t()
  def new(base_url, opts \\ []) do
    Transport.new(Keyword.put(opts, :base_url, base_url))
  end

  @doc """
  get calls get.
  """
  @spec get(Transport.t(), Types.Tag.t(), Transport.call_opts()) ::
          {:ok, Types.Doc.t()} | {:error, BowlineClient.Error.t()}
  def get(transport, %Types.Tag{} = input, opts \\ []) do
    Transport.call(transport, "get", :get, Types.Tag.to_map(input), &Types.Doc.from_map/1, opts)
  end

  @spec get!(Transport.t(), Types.Tag.t(), Transport.call_opts()) :: Types.Doc.t()
  def get!(transport, input, opts \\ []) do
    case get(transport, input, opts) do
      {:ok, value} -> value
      {:error, error} -> raise error
    end
  end
end
