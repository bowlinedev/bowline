# bowline_client

## 0.5.0

- First release: `BowlineClient.Transport` with `call`, `subscribe`, and `upload`, the `BowlineClient.Error` envelope, `Read` helpers, and `Rules` matching the Bowline server.
